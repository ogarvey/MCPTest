- CAT2D.EXE is a bare LE file with `LE` at file offset `0x0` (no DOS `MZ` stub). It reports CPU type `0x0002` (80386) and OS type `0x0001`; Ghidra currently has it loaded as `x86:LE:16:Protected Mode`, so the import mode is likely wrong for callgraph/decompile work.
- Existing MCP/Ghidra analysis in this state finds only 20 functions, string xrefs are mostly absent, and `functions_create` fails even at plausible prologues such as `0005:7170`.
- Useful string anchors despite the bad import: `load_and_relocate_le`, `_leload_binary`, `loader_init`, `Loading BINARY level %s.`, `Loading BINARY file %s`, `Loading PICTURE file %s`, `map/level%02d/lv%02ds%d.dem`, `catgun.map`, `charsel`, `leader`, `intro`, `_d.dat`, and `generate_sprite`.
- Graphics-related strings indicate at least two paths: an IFF/ILBM loader with `ByteRun1` decompression (`read_ilbm_plane`, `load_iff`, `BMHD`, `BODY`, `IDAT`) and a separate internal sprite-generation path (`generate sprite byte/word/long/quad_val overflow`, `draw_sprite`).
- Workspace now has `le_capstone_probe.py`, a standalone Python+Capstone probe that correctly parses this bare LE layout, maps object/offset to file offsets, and disassembles 32-bit code from the real entrypoint. On CAT2D.EXE the entrypoint maps to object 1 offset `0x42750`, VA `0x52750`, file offset `0x5A220`, and following the initial jump to `0x527C8` yields sane 32-bit startup code (`sti`, stack alignment, `int 21h` DOS version query, etc.).
