- `FUN_0005cb3f` is Borland stack-probe code, not Forgotten Saga asset logic.
- Real `.spb` / `.mob` archive bodies currently traced: `0001afde` open/init, `0001b08d` close, `0001b2fe` find by name, `0001b0ce` fetch subresource.
- Sample archive header facts: fixed header `0x408` bytes, `dword@0x404` high word = entry count, low word is archive-specific (`0x0053`, `0x008B`, `0x009F`, `0x00B7`, `0x00FF` seen in SPB), entry records are `36` bytes.
- Type codes observed: `.spb` = `0x2711`, `.mob` = `0x2712`.
- Verified on sampled `.spb` blocks: `u32 outerSize`, `u16 subCount`, `u32 subOffsets[]`, then `u32 payloadSize + payload` at each subresource offset.
- Verified `.spb` payload header shape: `u32 width`, `u32 height`, `u32 dataStart`, `u32 rowMetadata[height]`, with `dataStart = 12 + height*4` in sampled MAGIC and FACE payloads.
- Confirmed decode path: `diskres.spb` wrapper `00033512` -> dispatcher `0001105c` with `ECX=0` -> mode-0 decoder `00010243`.
- `rowMetadata[row]` is self-relative: row stream starts at `&rowMetadata[row] + rowMetadata[row]`.
- Mode-0 SPB opcodes are source-backed: `0x80..0xFF` literal run, `0x40..0x7F` transparent skip, `0x00` row end.
- The decoded payload is 8-bit indexed pixel data; palette selection is external to the SPB payload itself.
- Sample-backed palette finding: header palette aligns best at `0x06` as `255` `RGBX` entries; decoded sample corpus uses indices up to `254` and never `255`.
- The bytes at `0x02..0x05` and `0x402..0x403` vary by archive and remain unresolved.
- MOB top-level records are `40` bytes, not `36`: `name[32] + metadataOffset + dataOffset`.
- Binary-side MOB open/init is `00013e0c`: it reads `word@0x406` as entry count, allocates `entryCount * 40`, and loads the top-level table into `0x0bb88c`.
- MOB region fetchers are source-backed: `00014022` uses top-level `+0x20` (`metadataOffset`), `00014094` uses top-level `+0x24` (`dataOffset`).
- MOB `metadataOffset` regions are self-describing blocks: `u32 outerSize`, `u16 recordCount`, then `recordCount * (name[32] + u32 relOffset)` and record payloads.
- MOB `dataOffset` regions are also self-describing blocks: `u32 outerSize`, `u16 itemCount`, `u32 itemOffsets[itemCount]`, then `u32 payloadSize + payload` per item.
- MOB metadata record payloads are now partly source-backed: `u16 elementCount`, `u16 unknown`, then `elementCount * 20` bytes.
- Runtime code groups MOB metadata elements by the word at element offset `+6`, then uses that grouped value as the data-item index into the `dataOffset` region.
- `00013f54` is a metadata-record accessor inside a loaded metadata region, not a top-level metadataOffset accessor.
- Runtime-backed MISSILE placement split: `000202ed` and `000203de` use `(currentWord1 - firstWord1, currentWord2 - firstWord2)` for record-sequence playback, while `000204bd` uses `(currentWord1 - firstWord1, currentWord2 - firstWord2 - currentItemHeight)` for explicit-element selection.
- MOB metadata placement words `1` and `2` must be read as signed 16-bit values, not unsigned. `OBJECT.MOB` entry 428 record `ANI_0` proved this: unsigned X spans `4..65535`, signed X spans `-14..745`.
- `OBJECT.MOB` first-four-entry check: entries 0 and 1 have one metadata record plus three data items; entries 2 and 3 have zero metadata records and one data item each. That zero-metadata/single-item shape also appears at entry 86, so it is not a reliable "mask" marker by itself.
- The suspected `OBJECT.MOB` entries 2 and 3 are also multi-color decoded sprites (`11` and `21` palette indices used), and their names decode as normal objects (`의자`, `꽃병`). Current conclusion: no metadata-only mask discriminator yet.
- Runtime `OBJECT.MOB` consumer traced so far: `0x1d49c` opens `obj\object.mob`, `0x1d738` resolves per-entry metadata/data and placed bounds, and `0x2f06f` later walks the object table to stamp an 8x8 occupancy/collision map via `0x2f1cb`. No traced branch classifies entries as masks.
- `0x13e0c` caches only archive handle/count/entry table for MOBs; no header-palette state is exposed in the traced `OBJECT.MOB` path. This makes wrong-palette handling plausible.
- `OBJECT.MOB` entry 74 (`봉화대-커스`) uses palette indices `21`, `23..28`, and `75..79`, and those header-palette slots are all `(255,0,255)`. The magenta export is therefore consistent with using the archive header palette on a normal multi-color sprite, not with a mask payload.
- Later suspect OBJECT entries `371`, `405`, `412`, `461`, and `481` show the same low-index pattern. They are all low-only (`maxIndex < 128`), while normal-looking controls `376`, `381`, `385`, and `394` are all high-only (`minIndex >= 128`).
- OBJECT corpus split: among `457` entries with decodable sprite payloads, `338` are low-only, `109` are high-only, and `10` are mixed. Many low-only entries are environmental props/doors, which supports a scene-palette-dependent family.
- Metadata still does not distinguish the bad-looking entries: suspect and normal controls all use the same `1 metadata record + N data items` shape and the same `20`-byte element layout; nonzero element word4 is not a reliable classifier.
- Generic visual render path now traced: `0x2b66d` resolves a MOB item with `0x13f84`, then calls renderer dispatcher `0x1105c` with `ECX=0`. Mode `0` body `0x10243` clips and copies raw indexed bytes directly into framebuffer `0x0a0720` using row offsets at `0x0a55ba`; it does not consult MOB header palette bytes.
- `0x2b66d` still does not classify the bad-looking OBJECT entries specially. It always blits resolved object item data through `0x1105c(..., ECX=0)` and only conditionally adds a second overlay/resource blit when object field `+0x1f0 >= 0x14`.
- Global VGA palette upload is separate from MOB decode. `0x58693`, `0x58739`, and `0x58c85` write `0x300` bytes from working palette buffer `0x0f676c` to ports `0x3c8/0x3c9`. That working palette is derived from base buffers `0x0f646c` / `0x0f6502` by `0x58e20` / `0x58e50`, not from the currently loaded MOB archive.
- Scene/resource palettes feed a separate live pipeline: raw helper `0x14ac1` converts external RGBX data into DAC-ready RGB at `0x0bbe98`, post-processes it into scene palette buffers `0x0bc198/0x0bc498`, and can upload arbitrary ranges via `0x10194`. Call site `0x18010` does this immediately after `0x33512` loads a named `.spb` resource.
- Full scene palette upload helper `0x14a88` copies `0x0bc498 -> 0x0bc198` and uploads all 256 colors through `0x10194`; call site `0x28b55` uses it during a later state transition.
- Important model correction: traced `OBJECT.MOB` path `0x2b66d` is item/menu UI, not direct room-scene placement. It draws a fixed 2x3 grid from `DAT_000ea5f4` and returns through the item-screen state-machine tail at `0x2ad7a`.
- `DAT_000ea5f4` is populated by `0x2a874`, which clears the six-slot grid and filters the linked list rooted at `DAT_000bd2e8` via `+0x23c`, skipping objects with flag bit `0` set at `+0x1c0`. Caller `0x19be7` comes from a script/opcode dispatcher and passes a 16-bit mode/index used with table `0x0a8468`.
- Item-screen owner `0x2ada6` manages selected object `DAT_000ea614`, target/stack `DAT_000ea628`, quantities, and capacity/weight `DAT_000bd2dc`; it is consistent with inventory/item management rather than room-scene rendering.
- Corrected `0x157b4` caller model: opcode path `0x19ed0..0x19ef1` reads one signed 16-bit order word from the script stream into `EBX`, loads `EDX = [0x0c48a0]`, loads `EAX = [0x0c4878]` (named token), advances `0x0c4874` by `2`, and calls `0x157b4`. The earlier “two inline immediates” interpretation is wrong.
- `0x157b4` expands the named token through `0x162bf` into scratch buffer `0x0bd2f8`, then `0x16284` searches the live linked list `DAT_000bd2e8` for a node whose name at `node + 4` matches. On a match it writes `node[+0x1c4] = [0x0c48a0]`, uses the signed script word for `node[+0x1cc]`, and sets/clears bit `0` of `node[+0x1c0]` based on whether `[0x0c48a0]` is nonzero or zero.
- `0x0c48a0` is not just a pointer-like token; at `0x1a6fc..0x1a706` one opcode path writes it directly from a sign-extended script word. Treat it as an integer-like script category/state value. The world transition code later uses `node[+0x1c4] == 0` as the active-scene subset filter.
- This matches `0x2a874`, which only copies nodes into the visible six-slot item grid when `(node[+0x1c0] & 1) == 0`. So scene/state script explicitly names live entities, marks their visibility/classification in the current list, and the item UI later renders those entities under the already-live global palette.
- Actual live-sprite init path now traced: callers `0x167b2` and `0x1685e` build a live object/entity node, call `0x16d0c`, then insert the initialized node into the linked list via `0x16b10`.
- `0x16d0c` loads object definition tables from type/index table `0x0a59f4`, resolves two object-data pointers into `node + 0x224` and `node + 0x228` via `0x13fa8(node + 4, node + 0x224)`, frees the temp tables with `0x13ec9`, then caches the actual sprite payload pointer as `node[0x220] = FUN_00013f84(node[0x228], 0x0B) + 0xA8`. It also calls `0x17424(node[0x228])` for companion metadata via table `0x0a59fc`.
- So menu icons and at least one real live-sprite path share the same OBJECT bundle member: menu path `0x2b66d` resolves `node[0x228]` item `0x0B` on demand, while live path caches that same payload pointer in `node + 0x220`.
- Live render path `0x56426/0x56580` rebuilds visible slots from the current linked list and draws `node[0x220]` via mode-0 renderer `0x1105c`; if `node[0x1f0] >= 0x14`, it overlays shared grouped art from `FUN_0001b2ac(DAT_000ec9cc, 8, 1)`.
- No traced OBJECT consumer path so far (`0x16d0c`, `0x2b66d`, `0x56426`, `0x56580`) performs palette upload/conversion. Current best conclusion: the game uses real OBJECT.MOB sprite payloads for both menu and non-menu live sprites, but palette authority still comes from separate global scene/state palette machinery.
- Raw scans over `0x2a800-0x2bc00` and over the nearby dispatcher region `0x19500-0x1aa00` found no calls to palette loaders/uploads (`0x14a88`, `0x14ac1`, `0x10194`, `0x58693`, `0x58739`, `0x58c85`, `0x58e12`, `0x58e5f`, `0x58daa`). Current best conclusion: low-index OBJECT icons depend on the already-live global palette, likely inherited from the current scene/state, not on a per-object palette or an explicit object-to-scene mapping in the item UI path.
- Outer controller `0x17d46` dispatches on `0x0a5acc`, and script opcodes at `0x1a742/0x1a751/0x1a760/0x1a76f/0x1a7f2` write that same state (`8/c/a/f/9`). Subcontroller `0x17f8c` on `0x0a5ac0` contains the full-screen/palette upload path at `0x18000..0x18010`.
- Transition substate `0x18080` clears script context through `0x19123` (`0x0a5b3c = -1`, `0x0c4874 = 0`, `0x0c4870 = 0`), frees live nodes through `0x16aa4`, frees two other object/resource lists through `0x17664` and `0x1d5f5`, then returns to controller states `0x0a5ac0 = 7`, `0x0a5acc = 3`.
- Main gameplay/world tick is `0x1b46a`: it calls `0x1c8d0`, then if `0x0c50f4 == 0` calls the script interpreter `0x19594`, then if `0x0a5acc == 4` dispatches world substates through the `0x0c50f4` switch. This is the current ordered bridge from scene script execution into live world/object logic.
- No palette uploads were found in `0x1b46a`, `0x19594`, the `0x0c50f4` world substates, or the traced OBJECT consumers. Current model: palette changes happen in outer controller/resource states before gameplay (`0x0a5acc == 4`) resumes; live OBJECT sprites inherit that already-live scene palette.
- `FUN_00030693` is called from `0x32f06` with `EAX = [0x0eb740]` and `EDX = [0x0eb88c]` inside a separate raw controller keyed by `0x0ec9c8`; case `5` dispatches to this indexed OBJECT interaction routine after selecting the active target from table `0x0eb898`.
- Immediate pre-gameplay state is `0x17db7 -> 0x56df7`: when `FUN_00056df7` returns zero, the outer `0x0a5acc` controller writes `0x0a5acc = 4`. `0x56df7` draws shared art from `DAT_000ec9cc` through `FUN_0001b2ac(..., 0x0A, 0)` plus `0x1105c`, copies the work buffer back to screen, and polls input/state; it is a gate/overlay state, not a palette upload path.
- `FUN_00027e3c` (entry `0x27e3c`, state key `0x0e43d0`) is called from `0x32ed4` inside the same `0x0ec9c8` controller that later calls `FUN_00030693` at `0x32f06`. This proves the shared `0xf2860` target-state path and the indexed `node + 0x220` OBJECT interaction path are sibling states of one runtime controller.
- `0x0ec9c8` jump table `0x32af0` maps states `0..6` to `0x32b32`, `0x32b6b`, `0x32e29`, `0x32ec9`, `0x32ee9`, `0x32efb`, `0x32f0d`. State `1` walks candidate table `0x0eb898`, stores the chosen target in `0x0eb88c`, and selects follow-up state `3` (`FUN_00027e3c` via `0x0eb884`), state `4` (`0x292b0` via `0x0eb888`), or state `5` (`FUN_00030693` on `0x0eb88c`). State `2` is a wait/step gate using `0x0dfab0/0x0dfab4/0x0dfab8` and countdown `0x0eb87c`; state `6` calls `0x2f642` and resets `0x0ec9c8` to `0`.
- Separate outer controller at `0x31d98` dispatches on `0x0eb868` and explicitly owns the direct interaction branches: state `5 -> FUN_00030693(EAX = [0x0eb740], EDX = [0x0f2860])`, state `11 -> FUN_00027e3c(EAX = [0x0eb740], EDX = [0x0c936c])`, state `12 -> 0x292b0(EAX = [0x0eb740], EDX = [0x0c936c])`.
- `0x31d98` state `4` calls `FUN_0001ee24`; after that selector finishes, it returns index `EAX = [0x0c9368]`, and table `0x0a8ecc` routes `0..7 -> states 5..12` plus `8 -> state 3`. This is the concrete source-backed bridge into the late interaction states.
- In the higher-level object loop around `0x2e250`, the game calls `0x2f6dc`, stores the returned node in `DAT_000eb740`, and then calls `0x2f7a0`. The `0x0eb868` controller also exits through `jmp 0x2f7a0`, so both paths share the same live current-object model and object finalizer.
- FORGA second-table palette tails are now corpus-backed: all `105` palettes share the same diff pattern vs `BASEPAL.PAL`/OBJECT header palette. Indices `0..127` are scene-specific, indices `128..254` are stable except fixed patched indices `{153, 155..158, 175..183, 242}`, and the high range never varies scene-to-scene.
- Definitive OBJECT rendering rule for FORGA gameplay scenes: decode raw sprite indices, preserve the payload alpha mask, and apply the active scene tail palette (`*.tail-palette.rgbx.bin`). `OBJECT.MOB`/`BASEPAL.PAL` alone is wrong for low-index entries and can also miss the fixed patched high indices.
- Raw `256 x 4` RGBX scene palettes have a real opaque index `255`; transparency comes from OBJECT row opcodes, not from palette entry `255`.
- `Scordato --palette` now supports raw `255 x 4` and `256 x 4` RGBX palette files, so extracted FAM tail palettes can be used directly for OBJECT.MOB rendering.
- `Scordato --forga-scene <scene-or-resource>` now auto-resolves the matching FORGA second-table tail palette via `TestOutput\Scordato\FORGA\manifest.json`; if needed it regenerates the FORGA extraction from `Samples\MAP\FORGA.FAM`. First-table scene names are preferred because they correctly disambiguate space-before-NUL resource names such as `D-1 -> pec-in `.
- Scene/object association is now source-backed as a three-string transition key. Script helper `0x1ac91` reads three script strings into `0x0c4878/7c/80`, `0x1a9ad -> 0x1cbcd` copies them to persistent globals `0x0c5428/2c/30`, and world state `3` at `0x1ccaa` consumes that triplet via `0x1b340` before resolving `[0x0c5430]` through `0x1e837`.
- Correction: `0x1ac91` only snapshots string pointers from the script stream, and `0x1cbcd` only stages `EAX/EDX/EBX` into `0x0c5428/2c/30` plus `0x0c50f4 = 0x0d`; neither one derives filenames or selector pairs.
- Wrapper staging is still `first-string path -> second-string path -> anchor`, and the third string is still looked up directly by `0x1e758/0x1e7b7`, but the earlier claim that `0x1350e/0x135fa` directly look up the first two strings is retracted. Those helpers only gate on non-null/nonempty input and then consume already-published selector/blob state.
- `0x17664/0x176d4`, `0x1d5f5/0x1d665`, and `0x11af0` do cache cleanup/reset only; `0x1350e/0x135fa` do not load transition tables on demand.
- Alignment correction: the real local loader entries are `0x133f8` (`saga.scp`), `0x13455` (`saga.bin`), and `0x1349f` (local parse entry over `0x0bb850`).
- `0x133f8(EAX=filename)` loads a raw file into `0x0bb84c` and publishes `0x0bb86c/0x0bb870` as `0x24`-byte records. `0x13870(index)` returns `0x0bb84c + entry[+0x20] + 4`, while `0x13955/0x139ac` scan those same records via predicate helpers `0x5cdbf/0x5ce10`, not plain string compares.
- `0x13455/0x1349f` load and parse the second raw file into `0x0bb850`, skipping `count*0x80` and `count*0x44` before publishing `0x0bb85c/0x0bb860` as a selector pair where `0x0bb85c` is a 16-bit selector.
- `0x1350e` parses the selected blob at `0x0bb854 + 0x20` as `0x44 / 0x24 / 0x30 / 0x08 / 0x04` counted sections and publishes `0x0bb864/0x0bb868`.
- `0x135fa` parses the selected blob at `0x0bb858 + 0x20` as a fixed header plus counted sections `0x44 / 0x64 / 0x44 / 0x38`, then `0x1e724` publishes the active scene-record table at `0x0c9358/0x0c935c`.
- The normal runtime filename bridge is now concrete: `0x17b58` calls `0x133f8("saga.scp")`, `0x13455("saga.bin")`, then `0x1349f()`.
- Separate savegame bridge: `0x2c0a4` indexes `0x0a84d8` (`"saga00.sav" .. "saga04.sav"`), opens the selected save, rebuilds live objects through `0x2c572`, reads the remaining save tail into `0x0bb850` through `0x2c661`, then calls `0x1349f()`, `0x1350e(0x0eaed4)`, and `0x135fa(0x0eaef4)`.
- Remaining hidden upstream edge is now narrower: not the raw filename bridge, but the writers to the sibling staged-key pair `0x0c5420/0x0c5424`.
- There is also a sibling two-value staged-key path: `0x1c12b..0x1c138` loads `EDX = [0x0c5424]`, `EAX = [0x0c5420]`, `EBX = 0`, then calls `0x1b340` before the node-to-scene binding walk. Writers to `0x0c5420/0x0c5424` are still hidden and are now the cleanest remaining lead on the upstream bridge.
- `0x1c058` is now bounded as the room-level/no-anchor object-world mode: it is keyed by `0x0a5ba0 = 0x11..0x15`, enters `0x1b340(EAX=[0x0c5420], EDX=[0x0c5424], EBX=0)`, then filters `node[+0x1c4] == 0`, sorts by `node[+0x1cc]`, and binds live nodes to the active `0x2A`-byte scene-record table through `0x22c46`.
- `0x1cc10` is the anchored sibling: it consumes `0x0c5428/0x0c542c/0x0c5430`, resolves the anchor with `0x1e837([0x0c5430])`, and rebases live node coordinates in `0x1ccf7..0x1cd55`. That confirms the two-key and three-key paths are sibling room-level vs anchored placement variants of the same downstream transition system.
- Nearby object/world modes `0x1ba30`, `0x1bb00`, `0x1bd46`, `0x1bef0`, and `0x1c304` do not stage `0x0c5420/0x0c5424`. Current tightest conclusion is that the sibling pair is externally staged global state from a broader world/location setup path, not a local feeder next to `0x1c058`.
- `0x137b0` and `0x1b3e9` are downstream-only consumers of the current selector pair `0x0bb85c/0x0bb860`, not the filename/selector bridge.
- `0x1e73a` exposes that active scene lookup table as `count/base = 0x0c9358/0x0c935c`; `0x1e837` searches it as `42`-byte named records; `0x1e758/0x1e7b7` turn the matched record into scene tile coordinates. Hardcoded startup uses the same pattern via `0x1b369("고락스", "마을", "마돌에서")`.
- Immediate follow-up `0x1b392` only inspects the second-level record header at `0x0c50f8` and conditionally kicks the `0x24d34/0x24fa7` state path; it is not a per-object palette/tag resolver. Current strongest palette model: palette authority follows the active subscene selected by the first two transition-key strings.
- The nearby raw-second-string branch `0x1b856 -> 0x12df9` only copies the subscene name into working buffer `0x0bb6d0` and screen/state fields for an overlay/UI-style path (`0x12f4f` renders through `0x1105c`). It still does not expose a FORGA scene/resource identifier bridge.
- Scene/object binding continues after that lookup: `0x1c138..0x1c1f7` filters live nodes with `node[+0x1c4] == 0`, sorts them by `node[+0x1cc]` via `0x15b82`, then walks them alongside the active `0x2A`-byte scene records. `0x22c46(node, sceneRecord)` copies scene-relative coordinates into node fields `+0x2c/+0x30/+0x34`.
- This is the strongest current object-to-scene association model: script/state sets node fields `+0x1c4/+0x1cc`, the active three-string transition key selects the current scene record table, and the world transition code binds the filtered/sorted live nodes to those scene records. The palette context still follows the active scene, not any per-OBJECT palette tag.
- Strongest current object-to-scene association model: script/state names live nodes, assigns an integer-like category to `+0x1c4`, assigns an order/index to `+0x1cc`, the active three-string transition key selects the current scene record table, and the world transition code binds exactly the `+0x1c4 == 0` filtered/sorted nodes to those scene records. The palette context still follows the active scene, not any per-OBJECT palette tag.
- Current model: OBJECT sprites are associated with scene context indirectly through the active transition key and selected scene anchor, then inherit the already-live scene palette. No traced evidence yet shows a per-OBJECT-entry scene/palette tag.
- The remaining missing edge is the outer caller of that `0x0ec9c8` controller. Internal ownership is now clear (`0x32ed4 -> FUN_00027e3c`, `0x32f06 -> FUN_00030693`), but its top-level entry still does not surface as a normal direct xref in Ghidra.
- Practical conclusion: `OBJECT.MOB` magenta exports are very likely caused by using the archive header palette in the extractor, while the game uses a separate global palette state for indexed sprite rendering.
- `BASEPAL.PAL` is `0x432` bytes, not raw `768` bytes. Its `0x300` bytes starting at file offset `0x06` exactly match the embedded header palette bytes in `OBJECT.MOB`, `MAIN.MOB`, and `EFFECT.MOB`; it is shared base-palette evidence, not a distinct fix for `OBJECT.MOB`.
- `Scordato --palette` now accepts both raw `768`-byte VGA palettes and `0x2711/0x2712` files with embedded header RGBX palettes, so `--palette BASEPAL.PAL` works directly. Validation run on `OBJECT.MOB` completed successfully and recorded `embedded header RGBX palette from BASEPAL.PAL` in the manifest.
- `0x5d778` is memcpy with `dest = EAX`, `source = EDX`, `count = EBX`.
- `0x224cb` is a raw `0x300`-byte VGA DAC uploader. The proven direct raw palette path is `tit.pal -> 0x5a804 -> 0x224cb`.
- `0x1afd4` loads a named `.spb` header: full `0x408` header into `0x0c4c64`, top-level entry table into `0x0c5070`, optional full-header copy to a caller buffer.
- `0x3351f` wraps `diskres.spb`: it loads the header through `0x1afd4`, copies header bytes `0x06..0x405` (`0x400` bytes, including the embedded palette block and trailing dword) to a caller buffer, then resolves the requested entry. `0x17f99` uses that wrapper immediately before `0x1105c`.
- Practical palette rule now backed by runtime tracing: gameplay-style indexed draws take their palette context from the active scene/resource `.spb` header tail, not from `OBJECT.MOB` or `BASEPAL.PAL`. The exact gameplay DAC upload site is still unresolved, but extractor-side palette selection should follow the active scene/resource header.
- World/object startup is on a different branch: `0x18ee0` loads `static.dat` into `0x0c50d8` / `0x0c50d4`, and the world/object state machine at `0x1cbe4` later consumes that state.
- The earlier `0x1ab52 -> 0x1b314` world-loader interpretation was a mis-decoded raw byte stream and is retracted.
- In corrected world/object state `3`, when `0x0c50e0` is null the code loads `EAX = [0x0c5428]` and `EDX = [0x0c5430]`, calls `0x21325`, then calls `0x1e82a` and stores the result in `0x0c50e0`.
- Corrected `0x0c542c` anchor: around `0x1cbd3`, the state machine checks `0x0c542c`; if it is null it calls `0x1ab48(0)` and stores the return value in `0x0c50e8`, and if both `0x0c542c` and `0x0c50e8` are non-null it calls `0x1cced(EAX = [0x0c50e8], EDX = [0x0c542c])` before `0x1dc34`.
- `0x1cced` is placement/list logic, not palette logic: it walks a linked chain via `+0x23c`, clears bit `0x04` at `+0x1c0`, and rebases node coordinates at `+0x1d0` / `+0x1d4` using signed values from the active object at `+0x22` / `+0x24`.
- The live `map\forga.fam` file still disproves the old easy header model (`0x3176` at file offset `0`). The exact world-side container/record behind the gameplay branch remains unresolved.
- Runtime-backed `FORGA.FAM` layout is two-table, not one-table: first count `291` at `0x0004`, first table `0x0006..0x1235`, second header dword `0x00b85aa4` + count `105` at `0x1236`, second table `0x123C..0x18CB`.
- Both FAM tables are `char name[12] + u32 dataOffset`; `0x1af87` is the shared `strcmp` lookup helper over those `16`-byte records.
- `0x1ae15` is the scene loader: first-table lookup by requested area name, seek to `dataOffset + 4`, read embedded `12`-byte name + header fields, decompress first blob, then use the embedded name to look up and load a second-table blob.
- Shared FAM decompression path: `0x59c24` peeks a dword and rewinds, `0x59e9e` is a 4 KB window LZ-style stream with a leading decompressed-size dword.
- First-table records: leading dword matches `payloadSpan - 0x2C`; dword at `0x2C` is decompressed size; sampled records satisfy `field10 * field12 * 5 == decompressedSize`.
- Second-table records: leading dword matches `payloadSpan - 4`; dword at `+4` is decompressed size; runtime splits the decoded buffer at `size - 0x10400` and `size - 0x10000`.
- Real consumer meanings are now corrected: `0x14ac9` uses the `size - 0x10400` block as RGBX palette quads, while `0x115ba` and nearby xrefs use the final `0x10000` bytes as a `256 x 256` remap table. That tail is not tile graphics.
- First scene-buffer layout is now partly solved from code, not image guessing. `0x1ae15` fills `scene+0x20`, `scene+0x24`, and `scene+0x28`, then the caller republishes them as `bb634 -> a7718`, `bb638 -> a7710`, and `bb63c -> a7714`. Concrete consumers show: `bb634` is a byte attribute plane, `bb638` is the main `u16` tile-index plane, and `bb63c` is a second `u16` index plane that resolves through the `64`-byte `8x8` tile bank at `bb640`.
- Runtime-visible height is `field12 - 0x28`; the loader trims `40` rows before publishing scene dimensions.
- Real scene assembly path: `0x261a7` resets scroll state, `0x26727` builds an `8x8`-tile ring buffer from base tiles plus transparent overlay tiles, and `0x26560` / `0x2662d` blit a `320 x 240` window from that ring buffer. Attribute bit `0x80` triggers an extra per-pixel translation step; low nibble values are consumed elsewhere for category tests.
- `AREA02` validation: the attribute plane contains only `{0,1,3,4}` and does not set bit `0x80`, so a direct base-plus-overlay composite is valid for that sample without the unresolved translation selector.
- Full first-table FORGA scan: none of the `291` scene entries sets attribute bit `0x80`, so the unresolved remap translation branch is not required by any current first-table scene sample.
- Attr low nibble `1` is a static blocking/collision class: `0x11bc0` / `0x11c88` scan for it directly, and `0x2efc3..0x2f13f` copies attr-`1` cells into the occupancy scratch map at `0xeb5cc`.
- `0x2f1cb` stamps normalized object rectangles into that same occupancy map with byte value `2`, and `0x2f149..0x2f1c1` outlines active bounds with `3`; those are scratch-map values, not scene-attribute values.
- Attr low nibble `3` is the per-column foreground-mask threshold in `0x11cfe`: first attr-`3` row per column becomes the top edge of the queued second-plane foreground band.
- Attr low nibble `2` is the exclusion/hole class inside that queued foreground band: `0x11e82..0x11ee0` skips nonzero second-plane tiles when the current low nibble is `2`.
- `0x11e82..0x11ee0` writes queue type `1` records into the shared deferred blit queue at `0xa0724/0xa0728..0xa0738`; `0x101de` later flushes that queue, and type `1` uses `0x10dda`, an `8x8` transparent tile blitter that skips source byte `0xFF`.
- World render order is now pinned down from `0x1b554..0x1b59f`: `0x18ab0` clears/builds the shared queue with object type-`0` records and per-object `0x11cfe` foreground appends, `0x11476 -> 0x263ab(0)` draws the base scene window, `0x13099` draws additional direct mode-`0` sprites, and `0x11462 -> 0x101de` flushes the deferred queue. This proves scene foreground tiles can occlude queued actors / objects.
- `0x18c76 -> 0x11f05 -> 0x26259` is scroll/ring-buffer maintenance only, so there is still no surviving traced gameplay consumer for attr low nibble `4`.
- Attr low nibble `4` is best treated as the bulk terrain/platform surface class: it is always base-backed, often base-only, vertically self-contiguous, and commonly meets attr `1` laterally at terrain edges.
- No direct consumer currently distinguishes attr low nibble `5` or `6` either. They are rarer, more overlay-heavy variants of the same terrain family as `4`, not a separate traced runtime class. Heaviest current `5/6` scenes are `RAYBEOL`, `JH1`, `KHETCHA`, `KHETCHA2`, `D-E3`, and `TOW-2`.
- Archived probe samples now cover the remaining `5/6` families under `TestOutput/Scordato/FORGA_probe_samples`.
- Tooling fix: probe-only FAM runs now keep `.decoded.bin` outputs and `DecompressedOutputFile` manifest fields, so later scene classification still works after `--probe-scene`.
- Batch validation: all `291` first-table `FORGA.FAM` scenes now probe successfully with the current loader-derived compositor and emit `scene_composite.png` without recorded probe errors.
- Some room scenes (`D-9`, `D-2`, `C-1B`) were using the wrong second-table resource because FAM name matching trimmed spaces before the first NUL. Runtime `strcmp` keeps `"pec-in\0"` distinct from `"pec-in \0"`, and `"ray-in\0"` distinct from `"ray-in \0"`.
- Base-layer tile byte `0xFF` is opaque palette entry `255`, not transparency. Only overlay draws should skip `0xFF`.
- Correct room-scene bindings: `D-2` and `D-9` must use second-table entry `66`; `C-1B` must use entry `68`. The earlier lookup-table tile-id remap explanation is retracted.
- Base-layer tile byte `0xFF` is still opaque palette entry `255`; only overlay/foreground draws should skip `0xFF`.
- `SAN1..SKY3` and adjacent `SANSK1..3` are a distinct single-plane FAM family: attr plane all zero, overlay plane all zero, dense base plane only.
- Their current colors are not explained by a wrong external palette. The dominant repeated tiles in those scenes already resolve to the flat blue/green/olive colors seen in output under each resource's own runtime palette.
- Best current interpretation: these are special backdrop/panorama-style scenes, not ordinary attr/base/overlay walkable maps; exact caller-side use still needs runtime tracing.
- `Scordato` now emits `.fam` manifests with both table counts, shared LZ metadata, first-table `field10 * field12 * 5` checks, and second-buffer tail offsets under `TestOutput\Scordato\FORGA`.
- `FORGA.FAM` second-table names are not unique after trimming. Probe/resource lookup must preserve ASCII exactly up to the first NUL and keep space-before-NUL variants distinct.
- Probe status changed: `Scordato --probe-scene <name>` can now follow the loader-backed attribute/base/overlay layout and emit a source-backed scene composite when the attribute translation bit is unused. The old five-flat-plane interpretation is retired.
- Current split: `diskres.spb`/portrait-style wrappers are real but appear separate from the `static.dat`-driven world/object branch. The corrected `0x1cbd3` / `0x1cced` branch is placement-only, so the gameplay palette answer still lies outside both that path and the retracted `0x1ab52` story.
- Record choice happens in higher-level owner/state code before playback helpers run. Traced `13f54` callers pass explicit record indices, including `0`, `1`, `2`, and `4`.
- The old file-shape selector heuristic is retired. `Scordato` now treats explicit-element Y-corrected placement as the default primary MOB export. Optional sequence-relative exports are still available behind `--emit-sequence-relative`, and oversized strip canvases are skipped instead of crashing the run.
- Current `Scordato` defaults are lower-clutter: no raw `.bin` dumps, no strip PNGs, and explicit-aligned MOB frames are primary under `metadata/frames/`.
- Opt-in switches: `--emit-binaries` restores raw dumps and indexed/mask binaries, `--emit-strips` restores strip PNGs, and `--emit-sequence-relative` adds the older sequence-relative alignment under `metadata/frames-sequence/` and `*.sequence.png`.
- Runtime MOB data-item layout starts at the per-item `u32 payloadSize`; after that, the item contains a fixed `0xA8` bytes plus a variable tail whose size is stored at file payload offset `0xA0` (`runtime +0xA4`).
- Sample-backed MISSILE item sprite decode: width = `u16@file+0x04 + 1`, height = `u16@file+0x06 + 1`, `u32@file+0xA4` matches width, `u32@file+0xA8` matches height, row table starts at `0xB0`, and row streams use the same literal/skip/end opcodes as SPB mode 0.
- `Scordato` now exports per-item MISSILE sprite PNGs, runtime-relative named metadata strip PNGs, and aligned per-sequence frame canvases when the referenced item list resolves completely.
- `Scordato` now emits MOB metadata/data regions and the corrected named record structure into `TestOutput/Scordato`.
- CP949 decoding is useful for entry names; `MAGIC.SPB` names decode cleanly to Korean text.
- `.mob` inner semantics are only partly solved: the metadata/data region structure and part of the metadata-to-data bridge are source-backed, but the full meaning of each `20` byte metadata element and the fixed `0xA8` data-item header is still unresolved.
