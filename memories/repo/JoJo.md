JoJo SLES_025.99 notes:
- Ghidra address `0x8005661c` maps to raw SLES offset `0x46e1c`; use `fileOffset = address - 0x8000f800` for SLES table reads.
- PAC entry records are loader VM records: low 16 bits of `flags` = opcode, `data_length` = per-record transfer/stride length; payloads start at `0x800` and sector-align.
- `DAT_8005a0c8` IDs `0x0114..0x012d` map by exact length sequence to `P/KPLN00.PAC..P/KPLN19.PAC`, not `PLnn_HIT.BIN`.
- Per-character M group order is `PLnn.BIN`, `PLnnX.BIN`, `PLnn_HIT.BIN`, `PLnn_TKC.BIN`, `PLnn_TKD.BIN`.
- `jojoextract vm` inspects PAC loader VM records; `kpln-clut`, `vram-preview`, and `kpln-preview` generate code-backed KPLN CLUT/VRAM preview PNGs.
- KPLN recognizable sprites use renderer table entry `FUN_8001f9b4`: `0x0802` frame records contain bitmasks + descriptor offsets into `0x0801`; `FUN_8002078c` decompresses each referenced stream to one 16x16 4bpp tile. `jojoextract kpln-frames` defaults to this cache renderer.
- KPLN cached descriptor high byte: bits `24..29` are tile-relative CLUT selector for renderMode<4; bits `30..31` select `PTR_LAB_8005aa68` transform helpers: 0 identity, 1 vertical flip, 2 horizontal flip, 3 both. Caller object fields copied by `FUN_800209ec`: `+0x1d` clutMode, `+0x1e` clutBase, `+0x20` clutRowBase, `+0xb0` renderMode; `+0x0b/+0x12` derive orientation. `kpln-frames` exposes these after renderer.
- `FUN_80020b74` compact static records are 0x28 bytes: `+0x06` clutMode, `+0x07` clutBase, `+0x0c` clutRow, `+0x0e` frame, `+0x1d` asset slot. `kpln-contexts` auto-pairs `KPLNxx.PAC` with `M/PLxx.BIN`; PL files are MIPS overlays, so it scans code stores to context fields. For KPLN00/PL00 it recovers frames 0,1,3,4; full static records still need `DAT_80073e48` runtime buffer trace/dump.
- Player suffixes include `0A..0F`, not only decimal digits. For caller context overlays, side 0 uses `M/PLxx.BIN`; side 1 uses `M/PLxxX.BIN`. `_HIT`, `_TKC`, `_TKD` are same-character companions but separate hit/collision/table data, not default sprite context overlays.
- KPLN CLUT uploads: `FUN_800195c8` uses fixed `0x100`-byte palette stride only for `0x0803`; `0x0804..0x0806` use dynamic pool widths (`length / 4` words, `length / 2` bytes per palette). `KPLN03.PAC` has short `0x0806` length `0x80` and is the regression case. Filter recovered KPLN contexts to CLUT rows `0x1e0..0x1f7`; `auto` uses `cache-auto-strict` so frames without recovered caller context are skipped, not guessed.
- KPLN cached previews build both `FUN_800195c8` side placements from the same PAC. Side 0 writes rows `0x1e8/0x1ef/0x1f1/0x1f3..0x1f4`; side 1 writes `0x1e9/0x1f0/0x1f2/0x1f5..0x1f6`. High CLUT bases like `0xca`/`0xf0` can select side-1 rows through `FUN_8001f9b4`, so side-0-only CLUT reconstruction made non-empty strict frames render blank.
- PL overlays also carry animation scripts. `FUN_800268fc(object, script)` interprets records; handlers `FUN_80026be0`/`FUN_80026e1c` copy `script+0x02` to `object+0x10`, and `FUN_800209ec` masks that frame with `0x0fff`. Script record size is `commandByte & 0x2f`. `KplnRenderContextFinder` can follow overlay-local script pointers, but strict rendering only accepts script-backed frames when nearby setup also proves CLUT row/context. PL03 proves many extra frame indices, but not enough context yet.
- Script-context scan must match stores to the same object register passed as `$a0` to `FUN_800268fc`; otherwise a script pointer can be combined with context stores for a different object in the same overlay function.
- `FUN_80025394`/`FUN_80025640` initialize object `+0x20` to `0x01e8` and clear `+0x1d..+0x1f`; `FUN_800209ec` calls `FUN_80026b70` to resolve `object+0xb1` through `DAT_8008a2c0`/`DAT_8008a280` into live object frame/tile pointers. `FUN_80019914` fills those KPLN tables after loading `KPLNxx.PAC`; compact records index `DAT_8008a280` directly via `+0x1d`.
- `FUN_80019e4c` loads fileId `0x001a` and uploads `DAT_8010d800` to CLUT rows `0x1eb..0x1ed`; file-table length/LBA sequence maps `0x001a` to `P/COCKPIT.PAC`, entry `0x0101` populating the first `0x800` bytes. Do not inject this into KPLN rendering until call ordering/context is proven. Test injection did not fix PL03 weak-script blanks, so those frames likely need object/render-table source or inherited context tracing.
- `auto` is the unified PAC graphics export command: it inspects VM opcodes and runs every proven handler present. Current handlers: `0x0122`/`0x0123` compressed TIM, self-contained embedded TIM payloads, cached 4bpp frame assembly for records `0x0802`+`0x0801`+CLUT pool `0x0803..0x0807`, direct `FUN_8001ffd4` tile-word frames, opcode-class `0x0200` direct `LoadImage` entries as placed 4bpp grayscale previews, and raw VM-entry fallback/supplemental extraction. Embedded TIM extraction walks concatenated TIM streams but only decodes complete TIMs with internal image/CLUT blocks; it does not infer external CLUT pairing.
- Direct tile-word frames: `FUN_8001f324 -> FUN_80020b74 -> FUN_8001ffd4` is the static path; `FUN_800209ec` is the dynamic setup equivalent. `FUN_80020b74`/`FUN_800209ec` compute descriptor pointers as `mapBase + frame*0x0c`; `FUN_8001ffd4` consumes 12-byte records plus `ushort` tile matrices, skips `0xffff`, draws Sprt16 cells, uses `tileWord & 0x7ff` for texture coords and `tileWord >> 11` plus caller CLUT fields for palette selection. Focused auto probe: ARAKI exported 46 assembled direct frames; COCKPIT exported 95. Do not route packed `0x0102` map cells through this renderer.
- Direct VRAM + `0x0100` target profiles: `KS_CLRxx` (`02 01 01 02 01 01`) is code-backed by `FUN_8001bad4`, which sets `DAT_8008a280=0x80115800`, `DAT_8008a284=0x80116000`, texture bases `0x18/0x10` and `0x24/0`, then uploads CLUTs from `0x8010d800`/`0x8010dc00`; exact batch 39 succeeded, 1716 assembled PNGs, 0 blanks. In `02 01 01 01`, `KSAVE.PAC` validates as a single direct-frame triple (78 nonblank frames), while `PSJ/KPSJ/KSDM/KKAK/MTIT/SYO_BG/KTITLE/SS_DEMO` are packed-cell maps backed by `FUN_8002b62c`. Exact `02 01 01 01` auto-batch selected/succeeded 42 and produced 64 assembled packed-map PNGs with 0 blanks/low-colour. `GAL_SCR.PAC` still declines because map length `0x2c00` does not match traced `DAT_8005b2bc` dimensions. `TARxx` (`02 02 01 01`) stayed non-frame in exact batch 33; keep as direct VRAM previews + RAM payloads until a real consumer is traced.
- Packed maps: `FUN_8002b62c` uses object `+0x48` map pointer, `+0x44/+0x46` tile dimensions, and 32-bit cells where low halfword is CLUT coordinate and high halfword is texture-page bucket plus 16x16 `u/v`. Standalone renderer emits all size-equivalent `DAT_8005b2bc` candidates and prefers embedded TIM CLUTs over generic `0x0101` slabs when they fit all cells.
- TAR trace: `FUN_80017cb4` file table records at `DAT_8005661c + fileId*0x0c` prove `0x02d1 -> TAROT.PAC` (`0x52000`) and `0x02d2..0x02f2 -> TAR_00..TAR_20` (`0x21800`). `FUN_8001ae70` loads `0x02d1` and uploads `DAT_8010a000` as a `0x180 x 6` CLUT slab at row `0x1e2`, but `FUN_8001ae70` is not tarot-exclusive: its second live caller at `0x8001ac88` then loads file ID `0x0195`, which matches `P/KSTATUS.PAC` by exact size/count. No direct/simple table-driven `FUN_8001eda0` bridge for per-card TAR IDs has been found. The larger `0x8001aedc/0x8001af48` loader block is currently unreferenced: no direct `jal` xrefs, no raw pointer refs in `SLES_025.99` or `M/*.BIN`, and no entries in the live selector tables behind `FUN_8001e36c` / `FUN_8001e3f4`. Its fixed third load is file ID `0x0194`, which matches `P/KSS_MAP.PAC` by exact size/count, so it is not current TAR evidence. `0x8004fb78 addiu ...,0x02d0` is object pointer arithmetic, not a file ID. `TARDEV.PAC` (`0x02d0`, `0x1d800`) has a `0x0102` length `0x2c00` map-like payload outside the traced packed-map dimensions; file ID `0x01e4` is a distinct smaller `0x0f800` record.
- Direct-VRAM CLUT anchors: `FUN_8001a734`, `FUN_8001a890`, `FUN_8001a8fc` load file IDs via `FUN_8001eda0` into `DAT_8010d800` and upload `0x180 x 8` CLUT slabs at VRAM y=`0x1e0`; `FUN_8001a458` uploads a `0x180 x 1` slab at y=`0x1ee`. This proves a RAM-to-CLUT pattern, not yet exact COCKPIT/ARAKI/GAL_SCR assembly.
- `KMN08.PAC` is code-mapped, not loose: file ID table length sequence maps IDs `0x00f5..0x00f7` to `P/KMN07.PAC..P/KMN09.PAC`; `FUN_8001b29c` loads `0x00f6`, sets renderer table slots to `0x801f9b00`, `0x80118800`, `0x80115800`, and uploads CLUT slabs from runtime pool slots populated by `0x0803..0x0806`.
- `jojoextract report <file.pac|directory>` is the generic PAC classification layer: exact/family/candidate/unproven mapping evidence, VM profile, proven handlers, direct placed dimensions, palette-bank candidates as unpaired format facts, and pending parser work. Current aggregate before the packed-map demotion: 242 compressed-tim, 220 direct-vram, 163 raw-entries, 153 embedded-tim, 31 direct-vram-frames, 28 cached-frames, 26 kpln-clut; largest remaining raw-only profiles include `04 04 04` (92), `02 01 01 01` (42), `02 01 01 02 01 01` (39).
- Exact `01` VM profile is now extracted by `auto` when opcode is `0x0122`/`0x0123`: `FUN_800184c0` maps `0x0100` through `PTR_DAT_8005988c`, `0x0122 -> 0x80119800`, `0x0123 -> 0x8011f800`; `FUN_80025e64` calls `FUN_800267a8(src, src+0x40000)` then uploads the decompressed TIM image/CLUT with `LoadImage`. Batch output root used: `JoJo (SLES 025.99)/out/P_01_auto`.
- Full `auto-batch` over `P/*.PAC` with multi-TIM embedded support succeeded: 751 selected/succeeded, 0 skipped/failed. `out/P_all_auto_multitim` count check: 751 dirs, 10144 pngs, 615 tims, 373 embedded tims across 153 PAC dirs, 2415 raw bins, 509 raw manifests, 615 decoded TIM manifests. `KDGEVE00`: 40 TIMs/42 PNGs; `KBOINGO`: 13 TIMs/135 PNGs.
- Exact `08 08 01 01` profile embedded TIM batch succeeded: 102 selected/succeeded to `out/P_08080101_embedded_tim`, producing 102 embedded TIMs and 204 PNGs plus raw supplements. Representative `KOP_PL00.PAC`: `0x0106` is an embedded TIM; `0x0801` is frame-record-like and `0x0800` likely tile/compression-like, but the runtime consumer path is still unproven. Exact opcode shape `0800 0801 0101 0106` occurs in 44 PACs (`KOP_PLxx`/`KSYOxx`); broader class profile is 102 due to other low-byte `0x0100` opcodes such as KSD. `FUN_80019e4c`/`FUN_80019ef4` are not valid KOP evidence unless a file-ID bridge is found; candidate KOP_PL IDs by length/order are around `0x00fc..0x0113`.
- `KPLN03.PAC` cached-frame edge case: opcode set exists and `0x0806` length is `0x80`; dynamic `FUN_800195c8` CLUT widths now give two palettes. Strict output remains frames 0,1,3,4,16; PL03 animation scripts prove many more frame indices, but the full render context is still unresolved.
- Complete-output pass: `auto` now exports native `0x0400` VAB/SPU sound-bank parts (`FUN_800184c0` high-nibble branch + `SsVabOpenHeadSticky`), native `0x0800` runtime-pool parts (`DAT_80079928/DAT_800799b0`), and undecoded `0x0100` RAM payloads (`PTR_DAT_8005988c`) in addition to graphics/TIM handlers. Full `P/*.PAC` batch to `out/P_all_auto_complete_native`: 751 selected/succeeded, 0 failed; counts included 1838 PNGs, 615 TIMs, 112 `.vh`, 112 `.vb`, 948 `.pool.bin`, 571 `.ram.bin`. Aggregate report no longer has a `raw-entries` handler; remaining work is higher-level consumer tracing for direct-VRAM+RAM profiles and runtime-pool families, not raw PAC coverage.
