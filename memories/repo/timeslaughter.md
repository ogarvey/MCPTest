- `FUN_0001944c` is startup only; main VOL work happens in `FUN_000202cc` (palette-first family) and `FUN_0001fa04` (fighter `.vol` / `MIDGETPOWER`).
- DOS wrappers: `FUN_00031667` open read-only (`AH=3Dh`), `FUN_00031676` read (`AH=3Fh`), `FUN_00031648` close (`AH=3Eh`), `FUN_000344d7` lseek (`AH=42h`).
- Palette-first family starts with `0x300` VGA palette bytes, then the recovered block layout handled in `DOS/TimeSlaughter/TimeSlaughterVolProbe`.
- Tested palette-first files (`BG1.VOL`, `TITLEBG.VOL`, `SELECTBG.VOL`) end with `u16` little-endian MIDI-byte count + standard MIDI stream (`MThd` / `MTrk`); the probe now consumes these files to EOF.
- Probe `--export <dir>` for palette-first files now writes palette dumps, recovered block blobs, first-data slice export, TSV manifests, `trailing_music.mid` when that MIDI trailer is present, and ImageSharp-rendered PNGs for the current raw decoded background planes.
- Current high-confidence raw plane pairings: `fixed_block_04 + block_10 -> 640x100`, `fixed_block_08 + block_14 -> 640x100`, `block_0c + block_18 -> 320x150`.
- `FUN_00020bd8` is a row decoder using `0x80` skip runs and literal-copy runs; `FUN_00020b4c` then horizontally resamples those raw rows.
- `first_data` slices also decode with the same skip/literal row compression: header appears to be `[anchorX, width, height, 0xFF] + height*u16 row offsets + row data`.
- Probe validation: all 57 `BG1.VOL` slices and all 72 `TITLEBG.VOL` slices exported successfully to `first_data_slices_png` via ImageSharp.
- Main scrolling background compositor path is `FUN_0001d97c -> FUN_00020e84 -> FUN_0003f4f0`; this path uses the large block/table sets and `block_1c`, not the decoded `first_data` slices.
- Probe now exports `composite_view_zero_scroll.png` as a probe-level reconstruction of the recovered zero-scroll compositor call pattern; validated on `BG1.VOL` and `TITLEBG.VOL`.
- `FUN_0003f4f0` layering rule is now high-confidence: top layer A over lower layer B over `block_1c` background; the probe compositor was corrected from XOR-style overlap to this order.
- The zero-scroll preview now also ports `FUN_00020df4` using widened rows from `FUN_00020bd8` / `FUN_00020b4c`; `BG1.VOL` changed after this addition while `TITLEBG.VOL` stayed stable.
- `FUN_000202cc` computes widened-row state as `splitY -> obj+0x37c` and `rowCount = block14.height + 100 - splitY -> obj+0x378`; `splitY` comes from caller `EBX` when nonzero, otherwise from object `u16 +0x20`.
- `FUN_0001944c` feeds battle backgrounds from two paired tables: bg filename stems at `0x52470` and split overrides at `0x5251c`. Nonzero recovered pairs: `bg1/a0`, `bg2/a0`, `bg3/8c`, `bg4/8c`, `bg9/96`, `bg6/96`, `bg7/a0`, `bg8/9e`, `bg12/a0`, `bg5/a0`, `bg10/a0`.
- `FUN_00020df4` copies widened rows from `obj+0x370` into the destination starting at row `obj+0x37c` for `obj+0x378` rows; `FUN_00020d70` crops each widened row at `0x3c + shift`.
- `FUN_00020e84` lower-ground handoff is two-stage: if default split `obj+0x20` is below effective split `obj+0x37c`, `FUN_0003f71a` draws the gap from source row `obj+0x20-100` into destination row `obj+0x20`, then `FUN_00020bd8` decodes widened rows starting at source row `obj+0x37c-100` and `FUN_00020df4` places them at destination row `obj+0x37c`.
- `FUN_0001d97c` derives runtime horizontal parallax state as `DAT_00060308 = (DAT_00060328 * 8) / 10`; `FUN_0001ad4c` seeds battle scenes with `DAT_00060328 = 200`, so gameplay starts from a centered camera rather than a true zero-scroll crop.
- Probe now exports `composite_view_battle_init_scroll.png` using that battle-start camera (`foreground=160`, `mid=80`, `background=40`); after porting the table-driven split and exact lower-ground handoff, fresh BG1 battle-init exports no longer leave zero-valued pixels in rows `150..199`.
- The palette-first record path is now partially decoded: records are 10-byte entries, bytes `+0/+1` are `blob35a` descriptor offsets, bytes `+2/+3` gate compaction into the active list at object `+0x2F6`, and descriptor byte `+4` indexes the first-data slice pointer table at object `+0x26`.
- Probe export now writes `record_descriptors.txt`; current validation shows `BG1.VOL` has 3 active records and `TITLEBG.VOL` has 0.
- `FUN_00010874` / `FUN_0001060a` are the first-data slice blitters: they use bytes `+1/+2` as width/height, row offsets after byte `+4`, and skip runs as transparency; slice byte `0` is not used by this draw path.
- Probe export now writes `active_record_layer_relative.png`, `active_record_layer_report.txt`, and `composite_view_zero_scroll_with_active_records.png` when active records are present.
- Current validation: `BG1.VOL` active-record layer bounds are `x=-45..602`, `y=-28..53`; `TITLEBG.VOL` still has no active-record layer.
- `MIDGETPOWER` files start with `"MIDGETPOWER\0"`; helper readers are `FUN_0002d808` (u32 length + data) and `FUN_0002d87c` (same, adjusted length `-0x1E`).
- `FUN_000109a0` is a palette-remap pass over the same slice row format already used by palette-first `first_data`: bytes `+1/+2` are width/height, byte `+3` is `0xFF`, row data starts at `4 + height*2`, and rows use skip/literal control bytes.
- `Unknown` export still uses the grounded whole-file `FUN_000109a0` slice scan and writes `slice_scan.tsv`, raw bins, and PNGs.
- `FUN_0001fa04` first grounded fighter sprite table is now parsed directly: after the initial `FUN_0002d87c` block and the `4 + 6` relative-seek tables, the loader reads a `0x0F`-byte XOR-`0x8F` secondary name, then a `0x100`-byte remap table, a `u32[256]` offset table, a `u32[256]` length/presence table, a `u32 blobSize`, and the blob itself. `obj+0x7dd` is built from `blobBase + offsets[i]` when `lengths[i] != 0`.
- Corrected first-table offsets so far: `SAVAGE secondaryName=0x87100, mainRemap=0x8710F, blob=0x87A13`; `MOJUMBO secondaryName=0x5A7A7, mainRemap=0x5A7B6, blob=0x5B0BA`.
- MidgetPower export now writes grounded `primary_entries*`, `primary_offsets.tsv`, `primary_lengths.tsv`, `primary_blob.bin`, `secondary_name_*`, and `main_remap_*` instead of whole-file scan output.
- Canonical stage-palette preview still holds on grounded raw indices: `SAVAGE` structured primary entries use `117` raw palette indices, all stable across the `BG*.VOL` palettes, and `MOJUMBO` also exports correctly with `previewPaletteKind=stage-canonical:BG1`.
- Structured primary-table entry counts for all fighters (all referenced == decodable): `MOJUMBO=196`, `LAZARUS=191`, `PIERRE=185`, `PORTAL=183`, `CHI=173`, `VLAD=170`, `ASYLUM=169`, `SAVAGE=160`, `JINSOKU=163`, `SPICE=163`, `UG=150`, `BUDDY=149`, `RAVAGE=140`, `STAINE=123`. These are lower than old whole-file scan counts because they are strictly the entries owned by the primary pointer table in `FUN_0001fa04`, not all `FUN_000109a0`-style slices in the file.
- After the primary 256-slot sprite table, the loader reads 14 more `FUN_0002d87c` blocks into `obj+0xbe5` (8 bytes/entry = ptr+adjustedLen pairs). These are NOT sprites: they contain collision masks (FF/00 bitmaps) and animation state tables (small value ranges). Slice scanner finds 0 entries across all secondary blocks. Primary 256-slot table is the complete sprite bank.
- `MidgetPower` export now also parses and exports secondary blocks as `secondary_XX_off_*/raw.bin` (plus empty `png/` dirs when no slice entries found).
- Unknown family hits worth revisiting first: `MISC=100`, `FONT3=81`, `TITLE=13`, `SELECT=3`; `FONT`, `FONT2`, `GRIP`, `SOUND`, `SPIN`, and `TIMBRES` currently yield zero decodable slice-format hits.
- `--export` also writes `MIDGETPOWER` header dumps and a candidate VGA palette when `0x10 + dword@0x0C` looks like a 768-byte 6-bit palette.
- `--export` for MidgetPower now also writes `main_remap_256.bin`, `main_remap.tsv`, `main_remap_offset.txt`, `preview_palette.*`, and the grounded primary table artifacts.
- PNG export now treats palette index `0` as transparent in addition to any explicit coverage-mask transparency.
- `SELECT.VOL` / `TITLE.VOL` are not palette-first and not `MIDGETPOWER`; keep them as a separate family.
