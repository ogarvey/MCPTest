- CatGun: `FUN_0002a630` loads `map/*.dat`, then copies header fields to globals and rebases many internal offsets with `FUN_0002acd0`.
- Selector mapping confirmed: `1=intro`, `2=highscor`, `3=charsel`, `4=leader`, `5=<DAT_0006558c>`, `0=lv%02ds%d`.
- `DAT_000883e0` is a `0x28`-byte named resource table searched by `FUN_0002c150`; observed names include `INTROBCK`, `LEVELS`, `FONT`, `LEADER`.
- Confirmed on `Samples/map/intro.dat` and `Samples/map/charsel.dat`: extracted `.dat` files start with repeated `HZRD` and do not directly match the in-memory layout expected by `FUN_0002a630`; verify extraction source before writing a parser against them.
- `FUN_000110a0` is the main downstream DAT-content dispatcher; for DAT type `8` it uses `FUN_0002c470` and `FUN_0002c360` to scan rebased layer cell arrays in `DAT_000655a4[]` and resolve them through the named resource table at `DAT_000883e0`.
- `DAT_0006557c` behaves as banked palette data, not raw image/tile payload: multiple callers use `index * 0x300`, and `FUN_0003dd90` is a palette transition helper.
- If `0xF9C0` is the start of a common payload region in raw `.dat` files, the code does not reference it directly; payload access is indirect through rebased offsets and named resource entries.
- `FUN_0002c360` uses two dispatch tables: a static table at `0x3AF3C` with names like `PLT0..3`, `*_PICKUP`, `SCROLL_BORDER`, `KILL`, `END`, and powerups; and a second table chosen via `DAT_0006027c` through `0x60C0C`.
- `lv01s1.dat` contains second-stage level resource names from that code table, including `PIPE_DRIP`, `PIPE_LEAK_HOR`, `PIPE_LEAK_VER`, `SLIMER`, `STEEL_DRIP0`, `STEEL_DRIP1`, and `TEXTURE`.
- `FUN_00035000` resolves `INTROBCK`; its entry's `+4` field points to a secondary structure whose `+0x24` is stored in `DAT_00092764` and `+0x54` in `DAT_00092734`. `FUN_00036A00` uses `DAT_00092734` as four `0x400`-short planes for coordinate derivation.
- The extracted samples contain readable resource-name pools on disk (`intro.dat` around `0x12D82B`, `lv01s1.dat` around `0x106F40`), but the exact raw record format that expands into in-memory `0x28` resource entries is still unresolved.
- Level-1 second-stage table at `0x13F38` maps `SLIMER` to handler `0x144D0`; that stub calls `FUN_000136E0` and `FUN_0002C2C0`, then adjusts object state fields, so `SLIMER` is a spawned gameplay object/actor path.
- `TEXTURE` exists in the raw `lv01s1.dat` string pool but is not present in the level-1 `0x13F38` dispatch table; it likely uses a different lookup path than the `FUN_0002C360` second-stage table.
- Immediate-pre-pool raw-directory tests on `lv01s1.dat` failed for simple 16-bit/32-bit relative-name-offset and low-file-offset hypotheses, so the extracted file still does not expose a trivially direct on-disk form of the in-memory `DAT_000883E0` entry table.
- `FUN_00037640` mounts `GHX:/CATGUN.BLK` (fallback `CATGUN.BLK`), then reads `X:/POWER.CFG`; archive contents are exposed through a virtual filesystem layer.
- `FUN_0004ff50` reads an 8-byte BLK header into `DAT_0009ff4c/50`, then reads the remaining directory blob into `DAT_0009ff54`; `FUN_0004feb0` walks that directory in `0x30`-byte records.
- BLK directory record shape recovered from code: name at `+0x00`, file offset at `+0x28`, file size at `+0x2c`.
- Archive-hit DAT load path is raw: `FUN_0004fd90` -> `FUN_0004feb0` -> `FUN_0005574c` seek -> `FUN_00053da7` buffered copy. `FUN_00053da7` bottoms out in DOS read (`FUN_0005876f`), and seek bottoms out in DOS lseek (`FUN_000586fc`).
- No decompression/deobfuscation or wrapper stripping was found in the inspected BLK-backed `map/*.dat` load path; if current samples still mismatch `FUN_0002a630`, either the samples are not exact BLK entry bytes or the BLK directory points to an inner payload slice that current extraction did not respect.
- Direct BLK validation on `Samples/CATGUN.BLK`: header dwords are `0x21` entries and first-data-offset `0x640`; with `33 * 0x30 + 8 = 0x638`, the directory is padded/aligned to `0x640`.
- Parsed BLK records match the code-derived layout exactly: `+0x00..+0x27` path, `+0x28` offset, `+0x2c` size. Example: `map/intro.dat -> 0x27C840 / 0x144C9D`, `map/lv01s1.dat -> 0x4C7660 / 0x1DDBC5`.
- The loose extracted samples are not raw BLK slices even when sizes match exactly. Raw BLK `map/intro.dat` begins `02 00 00 00 ...`, while extracted `Samples/map/intro.dat` begins with repeated `HZRD`; same mismatch confirmed for `charsel.dat`, `lv01s1.dat`, and even `edfont.lbm`.
- For `map/intro.dat`, the extracted file's opening `HZRD` bytes first occur at offset `0x9c` inside the raw BLK entry, but the extracted file is not a simple rotation of the raw entry around that offset.
- Treat `Samples/CATGUN.BLK` as the canonical runtime source for extractor work; the current loose extracted files are a transformed or derived artifact, not the bytes consumed by `FUN_0002a630`.
- `DogKnife` now contains a BLK-aware raw extractor: `dotnet run --project DOS/CatGun/DogKnife/DogKnife.csproj` defaults to `Samples/CATGUN.BLK` and writes the canonical raw entries to `DOS/CatGun/TestOutput/BLK_Raw`.
- `DogKnife` now also has a first-pass DAT parser: `dotnet run --project DOS/CatGun/DogKnife/DogKnife.csproj -- --dump-dat <raw-dat-path>`.
- Header fields directly grounded from `FUN_0002a630` and now parsed in code: type `0x00`, variant `0x01`, dword `0x04`, bytes `0x08..0x0c`, counts at `0x10/0x14/0x18`, and raw table offsets at `0x1c..0x68`.
- The named resource table is now directly parsed from raw DATs: header `+0x4c` is the table offset, entry size is `0x28`, and the first dword of each entry is a string offset.
- Validation: raw `intro.dat` parses with 12 resources including `INTROBCK`, `LEVELS`, and `FONT`; raw `lv01s1.dat` parses with 58 resources including `PLAYER`, `PIPE_DRIP`, `SLIMER`, `STEEL_DRIP0`, `STEEL_DRIP1`, and `TEXTURE`.
- Next likely expansion points for the parser are the `0x1c` reference table, the `0x40`/`0x30` record table, the `0x58` layer block, and the per-resource payload behind entry field `+0x04`.
- `DogKnife` now also parses the `0x1c` cell-reference table and the `0x58` layer block from raw DATs.
- Code-grounded semantics: layer cells are 4-byte values whose low 16 bits index a `7`-byte table at header `+0x1c`; byte `+5` of each reference record selects the named resource entry in the `0x28` resource table at header `+0x4c`.
- For raw `lv01s1.dat`, the `0x1c` table spans `0x94..0x528`, yielding `167` full `7`-byte entries plus `3` trailing bytes; layer 0's max used cell-reference index is `166`, which fits exactly.
- The `0x58` layer block is `3` consecutive records, each `0x10` bytes of descriptor followed immediately by `width * height * 4` bytes of cell data; descriptor dwords `+0x08` and `+0x0c` are width and height.
- For raw `lv01s1.dat`, all three layers parse as `120 x 320`; non-zero cell counts are `106`, `39`, and `21` respectively.
- `DogKnife` now also parses shared payload groups behind resource-entry field `+0x04`.
- Non-zero `+0x04` pointers partition cleanly into `0x30`-byte blocks and many named resources alias the same payload start.
- Validated `lv01s1.dat` payload-group sizes: `PLAYER=29`, `PAW=14`, `PIPE_DRIP=14`, `TEXTURE=3`, `SLIMER=62`, `BLOBS=168` blocks.
- Validated `intro.dat` payload-group sizes: `INTROBCK=2`, `TITLE=1`, `DISPLAY=64`, `FONT=18` blocks.
- `FUN_00035000` reading `INTROBCK` payload offsets `+0x24` and `+0x54` now lines up with block 0 and block 1 of the parsed two-block `INTROBCK` payload group.
- `DogKnife` now also parses shared sequence groups behind resource-entry field `+0x08` as active bytes up to the last `0xFF` terminator, plus trailing spill bytes when present.
- Validated `lv01s1.dat` `+0x08` groups are short and shared across resource aliases; examples: powerups `00 01 02 03 04 04 03 02 01 00 FF`, `PAW` `00..0D FF`, `TEXTURE` `00 00 01 01 02 02 01 01 FF`.
- Validated `intro.dat` also fits the same `+0x08` parser surface; `PLINGY` has 2 trailing bytes (`48 5A`) after the last `0xFF`, so sequence parsing now trims at the last terminator instead of treating the full bounded span as active data.
- Grounded correlation: active `+0x08` bytes stay within the block count of the corresponding `+0x04` payload group, which strongly supports treating `+0x08` as frame/block selection order over the shared `0x30`-byte block arrays.
- `DogKnife` now has a first probe export command: `--export-textures <raw-dat>`.
- On raw `lv01s1.dat`, `TEXTURE` is now exported to PNGs using grounded block fields: width=`+0x08`, height=`+0x0C`, pixel-data offset=`+0x24`.
- `TEXTURE` block data offsets `0xC441C`, `0xC641C`, `0xC841C` advance by `0x2000`, exactly matching `64 * 128` bytes per block.
- The `TEXTURE` sequence `00 00 01 01 02 02 01 01` now drives exported frame order; exporter writes 3 block PNGs and 8 sequence-frame PNGs in both grayscale and a `palette_bank_03` probe set.
- Current palette probe: `lv01s1.dat` palette region `0x116CDC..0x118ADC` is `10 * 0x300` bytes; exporter treats it as 10 VGA-style palette banks and uses `((block.Value20 >> 16) & 0xFF) == 3` as the best current `TEXTURE` palette-bank candidate.
- `DogKnife`'s generic `--export-resource-planes` path is not code-proven and should be treated as disabled until a family-specific decode path is recovered from Ghidra.
- `PAW` raw-plane export was invalidated by runtime code tracing: `FUN_0003D030` binds `PAW` to callback `LAB_0003B6D0`, `LAB_0003B6D0` reaches `FUN_00013040`, and `FUN_00013040` stages the current frame's block `+0x24` pointer into a work item consumed by `FUN_00012EA0` / `FUN_00012FE0` instead of reading it as a raw `width * height` plane.
- The first `PAW` payload bytes at `0x18034` are `50 83 E0 03 5F FF 14 85 ...`, which is consistent with code-driven blit payload and inconsistent with a validated 8-bit indexed image plane.
- `PAW` resource entry in raw `lv01s1.dat`: `p04=0x1084DC`, `p08=0x114FC7`, `p0C=0x0`, `p14=0x107607`, sequence `00 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D FF`.
- `TEXTURE` still has the strongest visual probe, but its palette-bank source and exact draw path are not yet fully code-proven in Ghidra.
- Queue record shape recovered around `FUN_00012D40` / `FUN_00012EA0` / `FUN_00012FE0`: `+0x00` owner, `+0x04` payload pointer, `+0x08` destination, `+0x0C` auxiliary field, `+0x14` next/general-queue link, `+0x16` type.
- Type `1` records staged by `FUN_00013040` call `record[+0x04]` directly with `EAX = record[+0x08]`, proving that many block `+0x24` payloads are executable blit code.
- `FUN_0003BDC0` is a generic wrapper around the type-1 path (`FUN_0003FBF0`/`FUN_00040360` -> `FUN_00013040`) and has many xrefs, so the executable-payload family is shared beyond `PAW`.
- Type `3` records staged by `FUN_00013100` / `FUN_00013430` feed shared decoder `FUN_00028A00` with `ESI=record[+0x04]`, `EDI=record[+0x08]`, `EBX=record[+0x0C]`.
- `FUN_00028A00` format: source starts with `u16 segmentCount`, then repeats `u16 destSkip, u16 spanLength`; it applies a sparse remap over existing destination bytes using a 256-byte lookup page derived from `record[+0x0C] & 0xFFFFFF00`.
- `FUN_0002CA90` maps `DAT_000884BC = PLAYER` and `DAT_000884B4 = PILOT`; `FUN_00038130` calls `FUN_00013430(DAT_000884BC, ...)`, proving the shared type-3 decoder is used on `PLAYER`, and the same path also stages direct type-1 payloads from `PILOT` frames.
- Raw `lv01s1.dat` `Table40Offset` is `0x107BAC` with `1131` entries; its computed end `0x114FBC` matches the parsed global payload-block region immediately before the resource table at `0x11510C`, so the loader's `DAT_000883C8` loop is patching the same `0x30`-byte block space DogKnife parses behind resource `+0x04`.
- `FUN_0002ACD0` is the raw pointer rebaser: if a dword field is non-zero, replace it with `field + datBase`.
- `FUN_0002A630` block-type fixups: type `1` rebases `+0x24` then calls `FUN_000514D0`; types `3/5` rebase `+0x24` and `+0x28`; types `4/8` rebase `+0x24` only; types `2/9` are explicitly skipped by the fixup switch.
- `FUN_000514D0` patches executable payload signatures: `0x03E08350` (`50 83 E0 03`) gets dword `+0x08 = 0x4D31C`; nested `0xE8905550` (`50 55 90 E8`) gets dword `+0x04 = 0x4D4DC - ptr - 8`; alternate signature `0xE083F88B` (`8B F8 83 E0`) gets dword `+0x08 = 0x4D100`.
- Raw block checks: `PLAYER` and `PILOT` payload blocks begin with `50 83 E0 03 5F FF 14 85 ...`, matching the `FUN_000514D0` top-level signature; `PSTARS` blocks include both `BB C8 CC CF CC ...` pattern-writer blobs and `50 55 90 E8 ...` nested-signature blobs.
- In raw `lv01s1.dat`, the loader type is the low byte of block dword `+0x20`: `PLAYER` blocks `16..21` are the type-`3` subset (shared `+0x28 = 0x00107900`), while `PLAYER` blocks `0..15` and `22..28`, all `PILOT` blocks, and all `PSTARS` blocks are type `1`.
- Additional named render mappings: `FUN_00038E90` mixes shared type-3 `PLAYER`, direct type-1 `PILOT`, direct type-1 `PLAYER`, and spawned `PSTARS`; `FUN_00039880` is pure direct type-1 `PLAYER`; `FUN_00039C20` conditionally stages direct type-1 `CHARGE` via `FUN_0002D130`, then `PILOT`, then `PLAYER`.
- Type `6` records feed `FUN_00028C64`, a second shared graphics decoder that consumes a nibble-opcode byte stream through a 16-entry handler table at `0x00028CAB` and writes fixed patterns across rows spaced by `0x180` / `0x300`.
- The type-`6` queue contract is now exact from `FUN_00012EA0()`: it passes `width = block[+0x08]`, `height = block[+0x0C]`, `stream = block[+0x24]`, and the queue destination in `EDI` directly into `FUN_00028C64()`.
- `FUN_00028C64()` consumes exactly `((logicalWidth + 7) >> 3) * logicalHeight` bytes. Each source byte carries two 4-bit masks; each nibble expands four logical on/off bits into an `8 x 2` physical cell where each lit logical bit becomes a `2 x 2` kernel using indices `0x8A/0x87/0x84`.
- All 16 entries in the type-`6` handler table at `0x28CAB` are pure literal-store kernels over the local footprint; there are no external reads, branches, or hidden payload fetches inside the nibble handlers.
- DogKnife now exports loader type-`6` generically as transparent lit-pixel overlays under `type6_overlay`, preserving untouched substrate as alpha rather than guessing a background. Validation on `lv05s1.dat` cleared the pure `LED_GO` / `LED_HIT` / `LED_PAW` / `LED_PH` families and removed `0x06` from that DAT's unhandled-loader summary.
- Raw block-0 payloads for `PAW`, `NITRO_POWERUP`, and `1UP` in `lv01s1.dat` all start `50 83 E0 03 5F FF 14 85 ...`; their block `+0x28` pages near `0xB4300` / `0xB4600` look like translation tables rather than raw image data.
- DogKnife now parses the global `Table40` block table directly from DAT header `+0x40` / `+0x10` and reports per-payload-group loader-type distributions from block `+0x20` low byte.
- DogKnife command `--export-type3-probes <raw-dat> --resource <name>` exports conservative type-`3` diagnostics: `FUN_00028A00` remap coverage masks, raw/grayscale lookup-page dumps from block `+0x28`, and metadata instead of guessed final sprite pixels.
- Validation on raw `lv01s1.dat` / `PLAYER`: payload group `0x107BAC..0x10811C`, loader types `0x01:23, 0x03:6`, exported blocks `16..21`, single lookup page `0x107900`, block `16` coverage crop `34x18` matching declared `0x22 x 0x12`.
- `FUN_00038130` strongly suggests the type-`3` `PLAYER` remap family sits over earlier direct `PLAYER` output, not `PILOT`: direct `PLAYER` blocks `0..15` are `44x32`, while the type-`3` subset `16..21` is placed at `objY + 0x11` and overlaps the direct `PLAYER` vertical span for every recovered phase-table entry.
- `0x4D100` is an alignment-dispatched fixed `16x16` copy-kernel table fed by raw signature `8B F8 83 E0 03 FF 14 85 ...`; inline source bytes begin at payload `+0x0C`.
- `0x4D31C` is an alignment-dispatched variable-row dword-copy family fed by raw signature `50 83 E0 03 5F FF 14 85 ...`; outer stream begins at payload `+0x10` and uses `u32 rowCount`, then per-row `u32 destSkip`, `u32 encodedDwordCount`, and `(encodedDwordCount + 1)` dwords of source data.
- `0x4D4DC` is a scatter-write helper reached from raw signature `50 55 90 E8 ...`; helper data begins at payload `+0x08` and the code has separate dword-write and word-write phases.
- DogKnife command `--inspect-type1-probes <raw-dat> --resource <name>` now classifies type-`1` blocks by those recovered families.
- The former `BB ...` unknowns are recovered as `PatternWriter` stubs: straight-line immediate-writer code using `BB`/`B3` loads plus direct stores to `[eax+disp]`, including `89 98`, `66 89 98`, `88 98`, `88 B8`, `88 58`, `C7 80`, `C6 80`, and `C6 40` forms.
- Updated validation on raw `lv01s1.dat`: `PILOT` = `45` variable-row-copy blocks; `PLAYER` type-`1` subset = `16` variable-row-copy + `3` scatter-helper + `4` pattern-writer; `PSTARS` = `11` scatter-helper + `11` pattern-writer.
- Several direct `PSTARS` `0x4D4DC` blocks (`2`, `3`, `6`, `7`, `10`, `11`) have zero dword/word/byte phase counts after the helper stub, so `0x4D4DC` returns immediately and execution falls through into tail writer code at payload `+0x14`.
- DogKnife now has `--export-type1-renders <raw-dat> --resource <name>` for exact direct-family PNG export.
- The exact renderer currently covers `PatternWriter` and `0x4D4DC`, including the short word-store opcode `66 89 58 <disp8>`.
- Validation on raw `lv01s1.dat`: `PSTARS` renders `22/22` direct blocks; `PLAYER` renders `7/23` direct blocks (`22..28`) and intentionally skips the alignment-sensitive `0x4D31C` subset.
- `0x4D31C` exact semantics are now corrected from Ghidra: the copied helpers at `0x4D350/390/400/470` consume `rowCount` at payload `+0x10`, and each row's `destSkip` is relative to the end of the previous copied row.
- DogKnife's exact type-1 renderer now handles that progression correctly; validation on raw `lv01s1.dat`: `PILOT=45/45`, `PLAYER=23/23`, `PSTARS=22/22`.
- DogKnife now has `--export-type3-composites <raw-dat> --resource PLAYER`, which applies exact `FUN_00028A00` remaps over the corrected direct `PLAYER` base family using the recovered `FUN_00038130` selector and Y-offset tables.
- Current exact composite export writes the `16 base blocks x 12 remap phases = 192` matrix, not a claimed final animation order.
- Nearby PLAYER control code now grounds more of `FUN_00038130`: `FUN_00038600` maps input bits to `obj+0x36D` as directional variant `0..7`, and `DAT_00060F32[0..1] = [0,1]` keeps the PILOT row aligned with PLAYER facing.
- DogKnife `--export-type3-composites` now also writes exact `FUN_00038130` branch variants: `fun38130_special` (`1536` images) and `fun38130_cycle` (`1344` images), layering PILOT and small direct PLAYER accent blocks at the recovered offsets above the corrected direct PLAYER base plus remap.
- The remaining open gap for this path is state sequencing, not render semantics: we still have not fully proven the runtime temporal order of the base/remap/branch states.
- DogKnife now also has `--export-known-renders <raw-dat>`, which batch-runs the currently exact exporters across a DAT and writes a `known_render_summary.txt` coverage report. On `lv01s1.dat`, that now exports assets for `53/58` resources with `0` exporter failures and leaves only `8` resources with unresolved loader families.
- Current unresolved `lv01s1.dat` families are explicit: `BLOBS` (`type 3`), `DISPLAY` (`type 3/4`), `LEVINFO0/1/2` (`type 2/4`), `REACTOR` (`type 7`), and the `type 0` holders `END` / `STOP_BLOCK`.
- `BLOBS` is now tied to state `0x0B`: the BLOBS init stores the resource handle in `DAT_000884B8`, `FUN_0002CD50()` dispatches that state through `FUN_0003A7B0()`, and that helper only updates clip/viewport globals. The BLOBS state table entry resolves to handler slots `0x3A230, 0x3A2F0, 0x3A500, 0x3A8D0`.
- Manual decode of raw code at `0x3A2F0` now proves that this slot is the BLOBS composite renderer: it calls `FUN_00038130()` first, then in a 5-step loop queues one type-`1` BLOBS block and one type-`3` BLOBS remap block per pass via `FUN_00012E30()`.
- BLOBS switch table at `0x3A2D4` is now exact: state `0 -> 0x3A4CC`, state `1 -> 0x3A38D`, state `2 -> 0x3A3C2`, state `3 -> 0x3A4B2`. State `1` computes fresh base coordinates from `obj+0xE0/+0xE4`; the others land inside the shared tail and skip parts of that setup.
- Current proven BLOBS selection math from the shared tail: `slot = (obj[+0xDC] - 0x0A - 7*i) & 0x3F`, `phase = (initialPhase + 3*i) & 0x0F`, `directIndex = (0x20*i) + 8 + phaseTable[phase] + 8*(obj[+0x2E0 + slot] >> 1)`, and `remapIndex = directIndex & 7 = phaseTable[phase]`.
- `FUN_0003F300()` is the BLOBS visibility helper that clips the current `25 x 22` frame and writes the visible destination x/y through `0x9278C/0x92790`; the remaining BLOBS gap is now the exact semantics of state values `0/2/3`, not the queue or remap families themselves.
- `PLAYER` animation order is now grounded more tightly from raw bytes in `FUN_00038130()`: `obj+0x370 += DAT_00093924` with wrap `0x80000`, `obj+0x374 += DAT_00093924` with wrap `0xC0000`, and `obj+0x378 += DAT_0009393C` with wrap `0xC0000`. Base-block selection uses `obj+0x370 >> 16`, while remap phase / Y-offset selection uses `obj+0x374 >> 16`, so the base family and remap phase share one tick source but still remain separate counters.
- `0x38110` initializes the `FUN_00038130` state in special mode (`obj+0x79 = 7`), while `0x38B40` forces the cycle branch (`obj+0x79 = 0`) without resetting `obj+0x78/0x370/0x374/0x378`. `0x38E50` initializes the alternate `0x38E90` state family instead.
- The PLAYER state tables now support that model directly: one quartet is `0x38110/0x38130/0x38410/0x38B40`, another is `0x38E50/0x38E90/0x39270/0x394C0`. The remaining gap for exact end-to-end animation order is the runtime stride/seed of `DAT_00093924`, `DAT_0009393C`, and `obj+0x78/0x370/0x374/0x378`, not the local render formulas.
- Follow-up RE tightened that gap further: `DAT_00093924` still only shows READ xrefs in the current analysis surface, while `DAT_0009393C` has nearby stores at `0x35AC7` / `0x35B4D` that currently look value-preserving rather than a newly identified cadence source. The neighboring raw handlers at `0x37A30/0x37DA0` also use a different selector bank (`0x33C/0x360/0x364/0x365/0x368`) rather than the `FUN_00038130` bank.
- Raw bytes around `0x35AC7` / `0x35B4D` now confirm why those stores are not enough: they write the already-loaded `DAT_0009393C` value back after subtracting it from per-object countdown fields, so they are not the source of the PLAYER cadence cluster.
- Raw bytes also confirm the nearby entry points stay narrow: `0x38110` seeds only `obj+0x79` and byte flags, `0x38B40` only forces `obj+0x79 = 0`, and `0x38E50` initializes the alternate family/motion fields. None of them seed `obj+0x78/0x370/0x374/0x378`.
- The state-table structure is now explicit: `FUN_0002CC00/2CD50/2CE80/2D290` dispatch four slots from `fix_off32_0002C678 + state*0x10`, so the PLAYER families are concrete state IDs, not just adjacent code. Proven mappings include state `0x01 = {0x38110,0x38130,0x38410,0x38B40}`, state `0x04 = {0x38E50,0x38E90,0x39270,0x394C0}`, state `0x05 = {0x397E0,0x38E90,0x39270,0x394C0}`, state `0x0B = BLOBS`, state `0x0E = {0x38E50,0x38E90,0x39270,0x394C0}`, and state `0x0F = {0x37A30,0x37AC0,0x37DA0,0x38050}`.
- Follow-up xref checks narrow `DAT_0008849B` further: the only current writes are the DAT loader `FUN_0002A630()` and `FUN_00012C80()`, and `FUN_00012C80()` only preserves the existing byte while rebuilding shared buffers. In the current analysis surface, `DAT_0008849B` behaves as the DAT-type selector for the shared `FUN_0002CC00/2CD50/2CE80/2D290` dispatch tables, not as the live local PLAYER branch byte.
- The proven local FUN_00038130 branch control is instead object-local: `obj+0x79` selects special vs cycle behavior, and in the cycle path `obj+0x78` counts down, resets to `1` on underflow, and then increments `obj+0x79`. The remaining PLAYER timing gap is therefore the seed/cadence of `obj+0x78/0x370/0x374/0x378` and the `0x93924..0x9393C` timer cluster, not a runtime writer for `DAT_0008849B`.
- The loader-side write is now proven too: `FUN_0002A630()` reads the raw `map/<name>.dat` header and writes `DAT_0008849B = byte[0]`, `DAT_0008849A = byte[1]`, `DAT_000655BB = byte[0x0B]`, `DAT_000655BE = byte[0x0C]`, and `DAT_0006557C = base + *(base+0x54)`. `FUN_0002A500()` zeroes the palette-selector bytes first, then calls this loader.
- Palette work is now grounded enough for tooling: the raw DAT itself contains code-proven palette selector bytes, and state `0x08` startup also explicitly calls `FUN_0003DD90(DAT_0006557C, 0, 0x20000)`. DogKnife’s `TEXTURE` exporter can therefore emit code-grounded palette variants for bank `0`, header byte `0x0B`, header byte `0x0C`, plus the older block-field heuristic when present.
- Validation on `lv01s1.dat` sharpened the rule: its header `Type` is `0x0B`, header bytes `0x0B/0x0C` are `9/10`, palette bank count is `10`, and the exporter now emits `palette_bank_09_header_0B` plus the old `palette_bank_03_block_value20` heuristic. No bank-`0` variant appears there because the explicit state-`0x08` startup rule is type-specific, and no `header_0C` variant appears because bank `10` is out of range.
- The exact sprite exporters now use the same grounded palette model too. `Type1RenderedExporter` and `Type3CompositeExporter` preserve their grayscale outputs but also emit colored variants for any in-range code-proven header banks, with safe fallback to grayscale-only if the palette region is unavailable or malformed.
- Validation on `lv01s1.dat` now covers exact sprite output as well: `PLAYER` type-`1`, `PLAYER` type-`3`, and `PILOT` type-`1` all exported successfully with `palette_bank_09_header_0B`, confirming the new palette path is not limited to the `TEXTURE` probe exporter.
- The apparent "too dark" issue is now traced past conversion math. `FUN_0004CDA3()` writes `DAT_0009F968` straight to VGA DAC ports `0x3C8/0x3C9`, so the `0..63 -> 0..255` expansion in DogKnife is not the culprit.
- Type `0x0B` scene palettes are not flat bank selects. `FUN_0003DD30()` first zeros the live palette shadow; later `FUN_0003E760()` can trigger `FUN_00049640()`, which only schedules entries `0x00..0x7F` from `DAT_000655BB` and directly copies entries `0xE0..0xFF` from the raw `DAT_000655BE` source via `FUN_0003E080()`.
- Concrete export evidence matters here: `lv01s1.dat` `PLAYER` direct blocks use palette indices in the upper/mid ranges (`0xE0..0xEB`, `0xCB..0xCE`), so `palette_bank_09_header_0B` is only a comparison candidate for type-`0x0B` sprites, not the exact live palette.
- For `lv01s1.dat`, the exact `FUN_00049640()` raw source window for the `0xE0..0xFF` slice lies outside the parsed palette-bank region and is zero-filled, which proves some later palette writer still remains unresolved if the in-game scene shows bright upper-range sprite colors.
- DogKnife now exports palette-bank preview sheets for palette-aware outputs. Each export root gets `palette_banks/original_vga6bit` and `palette_banks/converted_8bit`, with one `bank_##.png` swatch sheet per parsed bank plus a `palette_banks/metadata.txt` manifest.
- The `FUN_00054EB0()` palette-script interpreter is now traced far enough to classify one path correctly: it dispatches opcode `0x0B -> FUN_00054E20()` for direct palette-segment writes, but the currently proven caller chain is `FUN_00036F40()` / `FUN_00036FC0()` on the `LEADER` mode path, not the plain `lv01s1.dat` level wrapper `FUN_00010E40()`.
- The sprite render queue is now proven palette-agnostic. `FUN_00012D40()` / `FUN_00012E30()` allocate 0x18-byte draw commands carrying function pointers, destinations, aux data, and a type byte; `FUN_00012FE0()` dispatches them without any palette-bank field. Type-3 helpers like `FUN_00028A00()` and `FUN_00046000()` manipulate indexed framebuffer bytes/remap tables, not palette banks.
- The DAT palette-bank table base `DAT_0006557C` is consumed by scene/state palette code (`FUN_000110A0`, `FUN_00010E40`, `FUN_0003B000`, `FUN_00049640`, `FUN_00020FC0`, `FUN_0004A820`, etc.), while the single live VGA palette shadow remains `DAT_0009F968` written by `FUN_0003E080()` / `FUN_0004CDA3()`. Current evidence therefore supports scene/state-level palette selection/composition, not per-resource palette-bank selection in the sprite renderer.
- `lv01s1.dat` is confirmed type `0x0B` (`0B ... 09 0A` in the header). The type-`0x0B` lookup byte in `FUN_000110A0()` is `0x01`, so the ordinary scene-start palette baseline is a full bank-0 transition via `FUN_0003DD90(DAT_0006557C, 0, 0x20000)` before later slice-specific updates run.
- Newly decoded type-`0x0B` handlers show later palette slices are dynamic: `0x14177` transitions low slice `1..0x7F` between banks 0 and 1, `0x14EC7` transitions low slice `1..0x7F` from bank 7, and `0x1527A` transitions slice starting at `0x60` with count `0x20` from `(byte[+0x9C] + 2)` banks.
- The nearby `0x14F80` call reaches `FUN_0003B1A0(0)`, which does not set `DAT_000602AA`; it calls `FUN_0003DE70(0x20000,0)` instead. So the observed startup/object sequence does not immediately arm the `FUN_00049640()` upper-slice path.
- The bank-0 type-`0x0B` startup baseline now validates on non-PLAYER graphics too: a `TEXTURE` export for `lv01s1.dat` emits the same shared `palette_bank_00_state0B_start` and `palette_bank_09_header_0B_low_slice_reference` variants, plus its existing local probe variant.
- Special render families do exist, but they are index remap/transform paths, not palette-bank selectors. `FUN_000473B0() -> FUN_00047410() -> FUN_00046000()` uses `DAT_00099574` as a remap table, and the `0x47CD0/0x47DBD -> FUN_00048500()` family uses `DAT_00099580/88/90/94` plus trig table `DAT_00011438`. Neither path reads palette-bank selectors directly during drawing.
- Palette choice is scene-global, not per-resource: `FUN_0002A630()` loads `DAT_0006557C` (palette table base), `DAT_000655BB` (header byte `0x0B`), and `DAT_000655BE` (header byte `0x0C`) once per DAT, and generic draw records still carry no palette-bank field.
- For type `0x0B` map scenes, `FUN_000110A0()` starts from full bank 0, then `FUN_00049640()` rewrites only `0x00..0x7F` from `DAT_000655BB` and `0xE0..0xFF` from `DAT_000655BE`-related data. In `lv01s1.dat`, `DAT_000655BE == 10` while static exported banks are `0..9`, so byte `0x0C` is not a direct static bank index there.
- DogKnife now emits a shared `default/` export tree for `TEXTURE`, type-`1`, and type-`3`. For type `0x0B`, `default/` is a best-effort hybrid palette: bank 0 baseline plus `0x00..0x7F` from `DAT_000655BB`, with `0xE0..0xFF` only taken from `DAT_000655BE` when that header byte is a valid static bank index; otherwise the tail stays on the bank-0 baseline.
- `--export-known-renders` on `lv01s1.dat` now validates the shared `default/` path across the whole exact exporter surface: 53 exported resources, 0 exporter failures, and `default/` trees present for the currently exact `TEXTURE`, type-`1`, and type-`3` outputs.
- `FUN_00049640()` is more tightly constrained now: it is called only from `FUN_0003E760()` when `DAT_000602AA != 0`, and `DAT_000602AA` is only armed by `FUN_0003B1A0(1)`. The traced `lv01s1` startup path still reaches the known `0x14F80 -> FUN_0003B1A0(0)` case instead. Combined with the raw-file probe showing zeroes at the direct `DAT_000655BE == 10` source range, this makes it less likely that `FUN_00049640()` explains the normal bright tail seen in-game for `lv01s1`.
- Exact loader type-`4` plane export now exists for the validated mixed-family resources `DISPLAY` and `LEVINFO0/1/2`. The exporter renders only the type-`4` blocks, skips sequence references to other loader families, writes the shared `default/` palette tree, and raises `lv01s1.dat` aggregate known-render coverage from 53 exported resources to 56 with 0 failures.
- `LEVINFO0/1/2` loader type-`2` is now grounded as a descriptor-crop family over the paired `256x200` type-`4` plane: `+0x00/+0x04 = x/y`, `+0x08/+0x0C = width/height`, and `+0x18/+0x1C` usually repeat the size but can be zero on a LEVINFO1 tail block. DogKnife now exports those exact crops and sequence-selected crop frames under `type2_crop`.
- After the LEVINFO type-`2` addition, `lv01s1.dat` coverage now reports `Unhandled loader types: 0x00:2`; the remaining unresolved resources there are the non-visual `END` and `STOP_BLOCK` holders rather than any `0x02`/`0x03`/`0x07` family.
- REACTOR's lone loader type-`0x07` block is now decoded and exported as an exact first-cycle effect family, not a sprite plane. State `0x14CA0` reaches `0x14F24`, which loads block104.Value24 into child handler `0x14F90`; `0x14F90` calls `FUN_0002A0CA()` on that pointer. `FUN_0002A0CA()` consumes 4096 fixed 10-byte particle records (`x_9_7`, `y_9_7`, `dx`, `dy`, `color`, `delay`) from the 0xA000-byte region at `0x5EE94`. DogKnife now exports the exact first 256-frame deterministic cycle for this family, clearing REACTOR's residual unresolved loader family and reducing `lv01s1.dat` aggregate unresolved-resource count from 8 to 7 with 0 failures.
- `DISPLAY` type-`3` is now split: block `48` is exact (`FUN_0002EFE0` case `1/2` -> `FUN_0002F5C0` -> `FUN_0002F6A0` copies `0..32` columns from type-`4` block `47`, then patches and applies block `48` through `FUN_00028A00`), while block `35` is only partially exact so far. The recovered caller path is `FUN_0002EFE0` case `3` -> `FUN_0002EDA0`, which clips `value >> 16` to `0x40`, computes `slotCount = (valueHi + 7) >> 3`, and draws exact direct blocks `27..34` at `+5` strides for the first `slotCount` positions before applying block `35` through `FUN_00028A00` over the remaining positions on unresolved preexisting framebuffer substrate.
- DogKnife `--export-display-type3` now exports that exact block-`35` filled-slot prefix family as `9` variants (`filled_slots_00..08`) under `DISPLAY/type3_runtime/block35_filled_slot_counts`, while still documenting the final block-`35` remap stage as substrate-dependent and therefore not a claimed fully rendered strip.
- The recovered DISPLAY layout path is tighter now too: `FUN_0002FA80()` first calls `FUN_0002F8B0()`, which draws block `16` at `(26,2)` and block `17` at `(224,2)`, then calls `FUN_0002EFE0()` twice through the static tables `0x2ECE8` and `0x2ED14`. Table `0x2ECE8` places the primary block-`35` strip at `(26,12)` while the same-state DISPLAY draws in that path land at `x=4`, `x=46`, and `x=76`, so the primary strip has no same-state DISPLAY substrate at all. Table `0x2ED14` places the secondary strip at `(224,12)`; helper block `17` overlaps only the leftmost `21` pixels there, leaving the rightmost `19` pixels still outside the current DISPLAY-owned draw path.
- DogKnife now has `--export-display-type3 <raw-dat>`, which writes `DISPLAY/type3_runtime` with `33` exact block-`48` width variants plus metadata for the still-open block-`35` path. `--export-known-renders` includes this exporter but intentionally still leaves `DISPLAY` unresolved as type `0x03`.
- Additional direct type-`1` PatternWriter forms are now validated in the wild: zero-displacement stores (`89 18`, `88 18`, `66 89 18`, `C7 00`, `C6 00`) plus disp8 imm32 stores (`C7 40 disp8 imm32`). These were the missing cause of several skipped `DISPLAY`/font/UI blocks.
- Validation after adding those forms: `charsel.dat` `DISPLAY` exact type-`1` export improved from `48/61` rendered blocks to `61/61`, recovering the previously skipped `7x9`, `5x8`, and `6x8` glyph/icon blocks.
- `charsel.dat` `CHARBIG` block `04` is still not explained by that fix. After the new PatternWriter coverage it now cleanly reduces to `declared=0x0` with `no pixel writes`, so it still does not behave like an ordinary fifth portrait block.
- DAT startup flag bytes at `DAT_00010E1F` still do not prove a bank-0 default for non-level types `0x02`, `0x06`, or `0x0D`; current grayscale/default-fallback handling for `intro.dat`, `charsel.dat`, and `evilbase.dat` is therefore not contradicted by the traced `FUN_000110A0()` startup path.
- Additional direct type-`1` PatternWriter form now validated from `highscor.dat` / `RBFONT1`: short byte store `88 78 <disp8>` (`mov [eax+disp8], bh`). That raises `RBFONT1` exact coverage from `34/43` to `42/43`; the sole remaining block `41` begins with `C3` and behaves like a dead placeholder/control stub.
- `highscor.dat` (selector `2`, DAT type `0x03`) has an exact post-startup full-bank palette transition: `FUN_0004A820()` later calls `FUN_0003DD90(DAT_0006557C + DAT_000655BE * 0x300, 0, 0x20000)`. DogKnife now uses header byte `0x0C` as the proven default live-palette bank for type `0x03` exports.
- `intro.dat` (selector `1`, DAT type `0x02`) also has an exact post-startup full-bank palette transition: an intro-state call at `0x35589` reaches `FUN_0003DD90(DAT_0006557C + 0x600, 0, 0x100)`-style setup, i.e. palette bank `2`. DogKnife now uses bank `2` as the proven default export palette for type `0x02` outputs.
- `charsel.dat` (type `0x06`) now has a proven bank-0 baseline too: recovered bytes around `0x482BF` show a later `FUN_0003E080(DAT_0006557C, 0, ...)` copy from bank 0, while bytes at `0x4724D` show another `FUN_0003E080()` copying `0x50` entries starting at live index `0x20` from `DAT_0006557C + bank * 0x300`, where the recovered bank bit currently selects `0` or `1`. DogKnife now uses bank 0 as the default type-`0x06` baseline and documents the later `0x20..0x6F` slice swap as still unresolved.
- DogKnife now has `--report-resource-coverage <raw-dat>`, which writes `resource_coverage_report.txt` under the DAT export root. The report cross-references per-resource loader types, current exact exporter support, DAT layer-cell references, and the current set of EXE-proven named lookups (`intro`: `INTROBCK`/`LEVELS`/`FONT`, `highscor`: `DIFONT1`/`RBFONT1`, `leader`: `LEADER`, selector-5 dynamic path: `LAFONT`).
- Exact type-`0x04` plane support is now widened beyond `DISPLAY` and `LEVINFO0/1/2` to the menu/background families whose blocks match the same direct indexed-plane shape: `INTROBCK`, `LETTERS`, `HALLBACK`, `PAWBACK`, and `TEXTURE_FRAMES`.
- Current non-level coverage after that widening: `intro.dat` now exports all 12 resources (runtime gap remains `INTROBCK` block `0x07` companion), `highscor.dat` exports all 34 resources (remaining unresolved family is `HALLBACK` block `0x07` companion), `charsel.dat` exports all 14 resources, `evilbase.dat` exports 28/29 resources with `TEXTURE_FRAMES` recovered and only `type 0x03` residuals left, and `leader.dat` remains a single EXE-referenced unresolved `LEADER` type-`0x07` family.
- The dynamic `selector 5` / `LAFONT` path in the `0x43xxx` region is also palette-active: `FUN_000430D0()` conditionally calls `FUN_0003B1A0()`, and the same region has a later `FUN_0003E080()` xref at `0x43D18`. Treat type `0x0D` (`evilbase.dat`) as script-driven palette state, not a proven static bank selection.
- `evilbase.dat` is now fully handled by the current exact-export surface: `RX7_FRAMES` exports as a same-resource type-`1` + type-`3` base/remap matrix, `SHADOW_FRAMES` exports as exact type-`3` remap data, and the refreshed `known_render_summary.txt` / `resource_coverage_report.txt` both report zero unresolved loader families with `Unhandled loader types: <none>`.
- `SHADOW_FRAMES` does not expose any same-resource direct base frames or sequence bytes; its exact exported form is the remap stream coverage plus lookup-page data rather than a claimed composited sprite. DogKnife now treats that resource-specific remap-data family as handled in both known-render and coverage reporting.
- `intro.dat` `OPTIONS` now exports as an exact same-resource type-`3` composite over block `13`, and its remaining type-`7` companion is now exported as raw companion data rather than left unsupported.
- `intro.dat` `INTROBCK`'s type-`7` companion is now exported as exact raw companion data under `type7_auxiliary`; this is grounded by the traced `FUN_00035000()` explicit lookup plus the non-visual companion consumption path through `FUN_00036A00()`.
- `highscor.dat` `HALLBACK`'s non-`LEADER` type-`7` companion is now exported as raw companion data under `type7_auxiliary`; the visible background remains the already-exported type-`4` plane family, while the companion bytes are preserved exactly without claiming a decoded full-screen render.
- After the companion-data export additions, refreshed non-level coverage is now clean in the current sweep: `intro.dat`, `highscor.dat`, and `evilbase.dat` all report zero unresolved loader families and zero runtime gaps in their latest summaries.
- `KnownRenderExporter`'s unresolved-loader aggregate now also correctly treats the already-supported `DISPLAY` type-`3` runtime exporter as handled, so intro/highscore-style summaries no longer carry a stale residual `0x03` for `DISPLAY`.
- `leader.dat` `LEADER` type `0x07` is now recovered as an exact script-render family, not a generic effect block. The proven path is `FUN_00036F40() -> FUN_00054FA0() -> FUN_00054EB0()`, with the current block using opcode `0x0B` for a full VGA palette upload and opcode `0x0F` for a `320x200` row-RLE image decode at runtime stride `0x180`.
- `INTROBCK` and `HALLBACK` do still each carry a residual type `0x07` companion, but they are not the same subtype as `LEADER`: `INTROBCK`'s companion is a non-visual auxiliary table consumed by `FUN_00036A00()`/`FUN_00035000()`, and `HALLBACK`'s type `0x07` block is not a LEADER-style script.
- `evilbase.dat` `RX7_FRAMES` now has exact same-resource type-`3` support in DogKnife: `36` direct type-`1` base blocks (`26x35`) plus `9` type-`3` remap blocks (`26x5`) export as a same-origin base-plus-remap matrix under `type3_composite`.
- `intro.dat` `OPTIONS` now has the same style of exact same-resource type-`3` support: direct block `13` (`122x80`) plus remap block `00` (`104x50`) export as a same-origin base-plus-remap composite under `type3_composite`.
- After those additions, `evilbase.dat` known-render coverage is `28/29` resources with only `SHADOW_FRAMES` still unresolved as type `0x03`, and its corrected resource-coverage aggregate now reports `Unhandled loader types: 0x03:2`.
- After those additions, `intro.dat` keeps full resource export coverage but its runtime-side unresolved set is now only the two residual type `0x07` companions (`INTROBCK` and `OPTIONS`), with the corrected aggregate reporting `Unhandled loader types: 0x07:2`.
- ResourceCoverageReporter's DAT-wide unhandled-loader summary must count only truly unsupported blocks, not whole loader-type groups. The fixed aggregate now filters by `(resourceName, loaderType)` support before grouping, which keeps mixed-family reports accurate after resource-specific exporter additions.
- The central frame-time source is now grounded past the PLAYER readers: raw code at `0x54900` is the timer ISR, which increments `DAT_00061CC0` at `0x54912`, maintains PIT state around `DAT_00061CC8/CA/CC`, and conditionally chains the prior timer vector. `FUN_000549C8()` is the PIT calibration/setup routine for that path.
- `FUN_0003E920()` is the engine-side bridge from the ISR tick source into frame pacing: it consumes `DAT_00061CC0`, computes/clamps `DAT_00060284`, and calls `FUN_0002C081()`, which self-modifies `FUN_0002C0C7()` by copying one of four code variants into `0x2C0C7`.
- The `DAT_00093920..DAT_0009393C` cadence cluster is still unresolved at the exact write site, but the current boundary is tighter: the whole block is zero-initialized in the image and, in the present analysis surface, every reference to `DAT_00093920/24/28/2C/34/38/3C` is a READ. No direct writer exists in the current xref model, so the remaining source is likely a hidden bulk/runtime path rather than an ordinary absolute store.
- Follow-up hidden-code reads tighten the semantics of that block even without the writer. `DAT_00093938` is now grounded as an angular delta: `FUN_0004A9E0()` reads it at `0x4A9ED`, adds it into `DAT_000613D0`, then uses the `0x1438` trig table through masked `0x3FF` indices.
- The same hidden scene/effect family also consumes `DAT_0009393C` as a countdown delta, not a source: the table-driven updater around `0x49B50` reads `DAT_00093938` up front, uses a computed jump table at `0x49B3C` (`0x49CFD/0x49D35/0x49DEE/0x49E31/0x49F19`), and multiple cases read `DAT_0009393C` to decrement object-local timers at `+0x74/+0x7C/+0x79/+0x7B`.
- `FUN_0004A820()` setting `object+0x74 = 0x49AAC` is now known to be a data-table seed, not a callback seed: `0x49AAC` sits inside a structured data block preceding the hidden updater, while code only starts later around `0x49B50`. That means this family is data-driven and still consumes the `0x93920..0x9393C` globals as preexisting scene-motion parameters rather than initializing them locally.
- A second hidden family in the low `0x20xxx` range now confirms the same ownership pattern. `FUN_00020FC0()` is a scene initializer reached from `FUN_000110A0()`; it seeds scene globals, then allocates an object with `FUN_000136E0(0x218C0, 0)`. Bytes at `0x218C0` decode as the scene callback itself, using scene globals `0x85D24/0x85D30` as script/state pointers and stepping with `DAT_0009393C` as a frame delta while spawning child effects.
- Within that same low-address scene package, the hidden reader cluster around `0x212CC/0x21372/0x21408` still does not write the cadence block. Instead it proves the meaning of the pair more sharply: `0x21372` builds a four-entry ramp `[DAT_00093920 + n * DAT_00093924]` on the stack, then object-local state around `0x60A00` consumes `DAT_0009393C` as a countdown delta and feeds trig-table sprite motion. Neighboring low-address readers at `0x20798` and `0x20AA0` show the same object-local subtract/add cadence pattern with `DAT_00093924` and `DAT_0009393C`.
- The practical boundary is therefore stronger than before: the `0x93920..0x9393C` block is shared scene/effect input consumed by multiple sibling object callbacks across at least two hidden families (`0x20xxx` and `0x49B50`), not a per-object local state block. The best remaining source candidate is a higher-level hidden scene bootstrap or bulk parameter load/copy path above those families, not any of the decoded callbacks themselves.
