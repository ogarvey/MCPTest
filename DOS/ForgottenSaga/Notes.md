# Forgotten Saga (DOS) Notes

## Filetypes of Interest
- `.spb`: Potentially sprite files.
- `.mob`: Generic image container, possibly for character models and/or objects.
- `.dat`: Generic container format, one is a smacker format, the others are unknown.
- `.img`: Possibly image files, but they are not in a known format.
- `.son`: Assumed audio file, only one present `Sound.son`

## Concrete Findings
- `FUN_0005cb3f` is not an asset routine. It is part of the Borland stack-probe / stack-check path. The tiny functions that directly call it, such as `FUN_0001afd4`, `FUN_000162bf`, `FUN_0001b083`, and `FUN_0001b0c4`, are 2-instruction wrapper stubs that push a frame size and then continue into the real body at the next address.
- The real `.spb` / `.mob` archive access path is currently visible at these post-stub entry points:
	- `0001afde`: open archive, read fixed header, allocate and read per-entry table.
	- `0001b08d`: close archive and free the table.
	- `0001b2fe`: search the entry table with `strcmp`-style matching.
	- `0001b0ce`: fetch a subresource by entry name and subresource index.
- `00013c90` is a file-open helper that passes the caller-provided filename with mode string `"rb"`. `00013cba` is a checked read helper that wraps the engine's buffered read function.
- `0005cd10` is a `strcmp`-style string compare. This confirms that the per-entry table contains null-terminated names, even though many names decode to non-ASCII bytes in the samples.
- The probe output confirms the entry names in sampled `.spb` archives are CP949-encoded Korean text rather than random binary. For example, `MAGIC.SPB` entry `0` decodes as `피라에로우`.
- `00033512` is a verified `diskres.spb` fetch wrapper. Its callers pass the returned payload to `0001105c` with `ECX = 0`, which reaches the first confirmed image decoder at `00010243`.
- `0001105c` is a mode dispatcher indexed by `ECX`. The jump table at `000a5548` points mode `0` to `00010243`, mode `1` to `0001040d`, mode `2` to `000105d0`, mode `3` to `000107d9`, mode `4` to `000109ea`, and mode `5` to `00010bde`.
- The real MOB-specific archive loader path is now source-backed as well:
	- `00013e0c`: open a `.mob` file in mode `"rb"`, read the fixed header, derive `entryCount` from `word@0x406`, allocate `entryCount * 40` bytes, and read the top-level entry table into memory.
	- `00013ec9`: free the current MOB top-level table and close the active MOB file handle.
	- `00014197`: search the loaded MOB top-level table by name and return the matching entry index.
	- `00014022`: fetch the full `metadataOffset` region for a top-level entry index.
	- `00014094`: fetch the full `dataOffset` region for a top-level entry index.
- `00014022` and `00014094` are the binary-side confirmation that sampled MOB top-level records really are `40` bytes and that the two trailing dwords are two separate region offsets. Both routines compute `entryIndex * 40`, then use either `+0x20` or `+0x24` as the per-entry seek target.
- `00013f01` is a name-based helper over a loaded MOB top-level table that returns the loaded `metadataOffset` region for a matching top-level entry name.
- `00013f54` is not a top-level accessor. Given a loaded metadata region in `EAX` and a metadata-record index in `EDX`, it returns `recordPayloadPtr = metadataRegionBase + relativeOffset` from the metadata region's `name[32] + u32 relativeOffset` table.
- `00013f84` is a generic `u32 outerSize + u16 count + u32 offsets[]` block accessor: for a loaded block pointer and sub-index, it returns `blockBase + blockOffsets[subIndex]`.
- Sampled `MISSILE.MOB` metadata record payloads now parse cleanly as `u16 elementCount`, `u16 unknown`, then `elementCount * 20` bytes. The runtime-backed data-item index is the fourth `u16` in each `20` byte element (element offset `+6`).
- Example: `MISSILE.MOB` entry `0` record `피라팔방향` resolves to eight data-item indices: `4, 9, 14, 19, 24, 29, 34, 39`.
- The runtime-side placement helpers are now partly recovered:
	- `000202ed` advances within a metadata record sequence and stores per-frame offsets as `(currentWord1 - firstWord1, currentWord2 - firstWord2)` using the second and third `u16` in each `20` byte element.
	- `000203de` is a sibling advance helper in the same local block that applies the same plain sequence-relative offset rule without the optional output bridge used by `000202ed`.
	- `000204bd` resolves an explicit element index inside a metadata record and stores `(currentWord1 - firstWord1, currentWord2 - firstWord2 - currentItemHeight)`, where `currentItemHeight` is read from runtime item offset `+0xAC` and matches sampled MISSILE file payload `u32@+0xA8`.
	- This means metadata words `1` and `2` are not best modeled as absolute top-left draw coordinates. They are signed 16-bit sequence-relative anchor terms, and one render path applies an additional Y correction from the current item.
- Record selection happens above those helpers, not inside the metadata record shape. Source-backed `13f54` call sites now show explicit metadata-record indices chosen by the owner/state code before playback: index `0` at `0002756a`, `00034879`, and `00035145`; index `1` at `00037ee1`, `0003aacf`, `0003ac2a`, `0003ad71`, `0003b0fa`, and `0003cfec`; index `2` at `00035cff` and `0003df9b`; index `4` at `000381d1`.
- `0002fa52` consumes an already-selected metadata record payload pointer, caches it, initializes the record state with `00020265`, then advances it with `000202ed`. This is the clearest confirmation so far that record choice is made in higher-level owner/state logic before the placement helpers run.
- `OBJECT.MOB` exposed the signedness directly: entry `428` (`새3`) record `ANI_0` contains `619` elements, and its X placement word spans `4..65535` if read as unsigned but only `-14..745` if read as signed. The signed interpretation is the only one compatible with a sane runtime canvas.
- `OBJECT.MOB` does not currently show a source-backed metadata flag for "mask" entries. The first two entries (`버꾸기 시계`, `똑딱시계2`) each have one metadata record driving three data items, while entries `2` and `3` (`의자`, `꽃병`) have no metadata records at all and only a single data item each.
- That zero-metadata/single-item shape is not unique to the user's suspected mask pair: only three `OBJECT.MOB` entries currently match it (`의자`, `꽃병`, `부서진감옥문`), which makes it a plausible static-object class but not a proven mask class.
- The suspected `OBJECT.MOB` entries `2` and `3` also do not decode like simple 1-bit or monochrome masks. Their only data items use `11` and `21` distinct palette indices respectively, and their top-level names decode as normal object names (`의자`, `꽃병`). Current conclusion: there is no reliable metadata-only mask discriminator yet.
- Runtime trace of the `obj\object.mob` consumer so far does not support a mask-specific path. The `0x1d49c` loader opens `OBJECT.MOB`, resolves per-entry metadata/data pointers through `0x13f54` / `0x13fc8`, computes placed item bounds in `0x1d738`, and later `0x2f06f` walks the resulting object table to stamp an 8x8 occupancy/collision map via `0x2f1cb`. No traced branch in that path classifies entries as masks.
- Alignment correction on that direct `OBJECT.MOB` world-object path: the real scene-object builder entry is `0x1d445`, and the selector-cache init beside it is `0x1d5b5`. `0x1d5b5(EAX = count, EDX = dwordTable)` stores `0x0c5440 = count`, stores the selector-to-top-level-index table pointer at `0x0c543c`, and allocates the zeroed per-selector bundle cache `0x0c5444`. `0x1d445(EAX = count, EDX = recordTable)` then allocates the zeroed live object table `0x0c5438` and consumes `0x44`-byte scene-object source records. The key local correction is that `source[+0x24]` is not the final top-level `OBJECT.MOB` entry index; it is a selector into `0x0c543c`, and the real top-level entry is only recovered later through `0x13fb6 / 0x13f4e`.
- The traced `OBJECT.MOB` consumer path also has no observed use of the archive header palette. `0x13e0c` only caches the archive handle (`0x0bb884`), entry count (`0x0bb888`), and copied 40-byte entry table (`0x0bb88c`). The setup/consumer routines above only fetch metadata/data regions and object bounds from that state.
- Entry `74` (`봉화대-커스`) strongly supports the wrong-palette hypothesis. It has one metadata record and five 16x48 data items, each using `15..16` distinct palette indices. Its active indices include `21`, `23..28`, and `75..79`; under the current header-palette assumption those entries are all `(255, 0, 255)`, which exactly explains the magenta-looking export without requiring the payload to be a mask.
- Follow-up comparison against the later suspect `OBJECT.MOB` entries `371`, `405`, `412`, `461`, and `481` shows the same pattern, and the contrast with normal-looking entries is now corpus-backed. The suspect set is entirely low-index: `74 -> 0..82`, `371 -> 0..127`, `405 -> 0..110`, `412 -> 0..122`, `461 -> 0..109`, `481 -> 0..87`. In the same pass, normal-looking controls `376`, `381`, `385`, and `394` are entirely high-index: `376 -> 128..247`, `381 -> 128..246`, `385 -> 128..212`, `394 -> 129..193`.
- Broader `OBJECT.MOB` sweep over `457` entries with decodable sprite payloads: `338` are low-only (`maxIndex < 128`), `109` are high-only (`minIndex >= 128`), and only `10` are mixed. Early low-only examples are heavily environmental (`문*`, village-house doors, fireplace, etc.), which supports the reading that low-index `OBJECT.MOB` art is authored against the active scene palette, while the high-index family is much closer to the shared/base-palette-looking object set.
- Metadata does not currently separate those two families. The suspect entries above and the normal controls all still use the same top-level shape (`1` metadata record + `1..7` data items) and the same `20`-byte element layout `(word0=1, word1, word2, word3=dataItemIndex, word4?, rest usually zero)`. Nonzero `word4` is not a discriminator either: it appears in broken-looking entries like `74`, `405`, and `461`, but also in normal-looking entry `394`.
- Current best explanation for the bad-looking exports is therefore palette context, not a special metadata/render class inside `OBJECT.MOB`: the traced visual path still pushes raw 8-bit indices through mode `0`, and these suspect entries are simply landing on the wrong global palette when decoded offline with the archive/base palette.
- The generic MOB visual path is now partly source-backed. `FUN_0002b66d` resolves a MOB data item with `0x13f84`, places the returned item payload in `EBX`, and calls the generic blitter dispatcher `0x1105c` with `ECX = 0`. That path confirms that mode `0` is a real on-screen renderer, not just an offline decode helper.
- The mode-0 renderer at `0x10243` does not read any palette from the MOB payload or archive header. It clips the sprite, computes the destination inside the global framebuffer at `0x0a0720` using row offsets from `0x0a55ba`, then copies the literal source bytes directly with `rep movsd` / `rep movsb`. In other words, MOB/SPB sprite data reaches the screen as raw 8-bit indices.
- `FUN_0002b66d` still does not show a metadata-family split for the bad-looking `OBJECT.MOB` records. It iterates active object slots, resolves item `0x0B` from each object's data block, and always calls `0x1105c(..., ECX = 0)` for the primary blit. If object field `+0x1F0 >= 0x14`, it resolves a second resource through `0x1b2ac(0,8)` and blits that too, but there is still no branch on low-index versus high-index palette usage and no per-entry palette source read from `OBJECT.MOB`.
- Palette upload is a separate global video path. `0x58693`, `0x58739`, and `0x58c85` write `0x300` bytes to VGA DAC ports `0x3c8` / `0x3c9` from the working palette buffer at `0x0f676c`. That working buffer is derived from a separate base palette buffer at `0x0f646c` / `0x0f6502` (`0x58e20`, `0x58e50`) rather than from the currently loaded `.mob` archive.
- The scene/resource palette path is now more concrete too. The raw helper at `0x14ac1` converts external RGBX palette data into DAC-ready `3`-byte RGB entries at `0x0bbe98`, post-processes that into working scene palette buffers at `0x0bc198` / `0x0bc498`, and can optionally upload an arbitrary palette range through `0x10194` (`out 0x3c8/0x3c9`). The call site at `0x18010` proves the mechanism: after `0x33512` loads a named `.spb` resource, the code draws the image through `0x1105c`, then calls `0x14ac1(EAX = 0, EDX = 0xFF, ECX = 0, EBX = paletteBlock)` to convert a full `255`-entry external palette into those shared scene buffers.
- The companion helper at `0x14a88` copies `0x0bc498 -> 0x0bc198` and uploads the full `256`-color DAC through `0x10194`. A later call site at `0x28b55` uses that full-upload path during state changes. Current source-backed reading: the engine has a live scene/resource palette pipeline entirely separate from `OBJECT.MOB`, and low-index OBJECT entries are therefore plausible consumers of whatever scene palette is active when they are drawn.
- Current source-backed conclusion: even before we pin down the exact world-object draw call for `OBJECT.MOB`, the runtime render path already proves that `OBJECT.MOB` header palette bytes are not the palette authority for on-screen object rendering. The extractor's current header-palette assumption is therefore very likely the cause of the magenta outputs.
- Important correction from deeper tracing: the `OBJECT.MOB` path at `0x2b66d` is not a direct room-scene object renderer. It draws a fixed `2 x 3` grid using entries from `DAT_000ea5f4`, with screen positions computed as `x = col * 0x28 + 0x14`, `y = row * 0x23 + 0x28`, and it jumps back into the same item-screen state machine tail at `0x2ad7a` when finished. That makes this a menu/UI draw path, not free placement inside the world scene.
- The active `DAT_000ea5f4` grid is populated by the real entry at `0x2a874`. That routine clears the six-slot array, then walks the linked list rooted at `DAT_000bd2e8` via object field `+0x23c`, filtering out entries whose flag byte at `+0x1c0` has bit `0` set. The same routine stores a script-provided mode/index in `DAT_000ea5b8` and looks up a companion pointer table at `0x0a8468`. This is concrete runtime association, but it is association with the current live object/entity list and a menu mode, not direct binding from an `OBJECT.MOB` record to a specific room-scene asset.
- The caller at `0x19be7` makes that even clearer. It lives inside an opcode/script dispatcher: it reads a `16`-bit parameter from the script stream at `0x0c4874`, advances the stream, and then calls `0x2a874`. So the item screen is entered from script/state logic with a small mode argument; we have not found evidence here of a per-scene palette selector coming from `OBJECT.MOB` metadata.
- The owning state machine at `0x2ada6` is item-management logic rather than room rendering. It tracks the selected object from `DAT_000ea614`, a target/stack cursor at `DAT_000ea628`, quantity counters at `+0x1c/+0x20/+0x24/+0x28`, and remaining capacity/weight in `DAT_000bd2dc`; it calls `0x27ba2(DAT_000ea614, *(DAT_000ea628 + 4))` when the action completes. That supports the reading that the traced OBJECT screen is a menu over current entities/items.
- A direct palette check over the whole item-screen region (`0x2a800-0x2bc00`) found no calls to `0x14a88`, `0x14ac1`, `0x10194`, `0x58693`, `0x58739`, `0x58c85`, `0x58e12`, `0x58e5f`, or `0x58daa`. A second scan over the nearby script-dispatch region around the `0x19be7` caller (`0x19500-0x1aa00`) also found no direct palette-helper calls. Current evidence therefore favors this stricter conclusion: the bad-looking low-index `OBJECT.MOB` entries are not being scene-bound by a special object-to-room mapping in this code path; instead, the item/menu renderer appears to inherit whatever global palette is already active when the screen is opened.
- Post-wrapper decode closes the local item-screen handoff more tightly. The real body behind `0x2a874` starts at `0x2a87e`: it uses `EAX` as an index into `0x0a8468`, stores the selected companion-table pointer in `0x0ea5b4`, stores the mode/index in `0x0ea5b8`, clears the six-slot grid at `0x0ea5f4`, walks the live node chain rooted at `0x0bd2e8` via `+0x23c`, copies only nodes with bit `0` clear at `+0x1c0`, runs `0x125f6` over the companion table, calls `0x122a0`, then writes `0x0c50f4 = 7` and jumps into the item-state tail at `0x2ad7b`. There are still no palette-helper calls anywhere in that entry path.
- The script-side entry is now source-backed from the real interpreter body too, not just the earlier summary. In `0x1959e`, the opcode window at `0x19be0..0x19bec` simply advances `0x0c4874` by `2`, calls `0x2a874`, and then jumps out through `0x1a965`. So this opcode does not stage any palette/resource key alongside the menu entry; it is an immediate handoff into the already-live item/menu state.
- The missing association mechanism is now more concrete than that earlier summary. In the opcode path at `0x19ed0..0x19ef1`, the interpreter stores the current script pointer in `0x0c4874`, reads one signed `16`-bit immediate from `[scriptPtr]` into `EBX`, loads `EDX = [0x0c48a0]`, loads `EAX = [0x0c4878]`, advances `0x0c4874` by `2`, and then calls `0x157b4`. So the `0x157b4` caller takes one inline order word plus a separately maintained script-controlled token/global in `0x0c48a0`; the earlier “two inline immediates” reading is retracted.
- The `0x157b4` path itself is now source-backed in detail. It first calls `0x162bf`, which expands/copies the script token into scratch buffer `0x0bd2f8`, then calls `0x16284`, which walks the linked list rooted at `DAT_000bd2e8` and compares that expanded string against each node at `node + 4`. On a match, the routine uses `EDX = [0x0c48a0]` as the value to assign to node field `+0x1c4`, and uses the signed script word from `EBX` as the value to assign to node field `+0x1cc`.
- `0x157b4` has two distinct behaviors depending on whether the script-controlled value in `0x0c48a0` is zero. When it is nonzero, the routine shifts down later nodes with `+0x1c4 == 0` whose current `+0x1cc` exceeds the target, then writes `node[+0x1c4] = [0x0c48a0]`, `node[+0x1cc] = orderWord`, and sets bit `0` in `node[+0x1c0]`. When `0x0c48a0 == 0`, it instead places the matched node after the highest currently active `+0x1c4 == 0` order, writes `node[+0x1c4] = 0`, increments `node[+0x1cc]`, and clears bit `0` in `node[+0x1c0]`.
- This is the corrected source-backed link between the script/state layer and the live object list consumed by both the item UI and the later scene binder. `0x0c48a0` is also now constrained further: at `0x1a6fc..0x1a706`, one opcode path reads a signed `16`-bit word directly from the script stream and stores it to `0x0c48a0` before calling the `0x158b6` helper family, so the field is definitely being used as an integer-like script category/state value, not just as a pointer token. Its exact gameplay label is still unresolved, but it is clearly the value later tested by the world transition code's `node[+0x1c4] == 0` active-scene filter.
- The bit usage lines up with the later menu scan. `0x2a874` only copies nodes into `DAT_000ea5f4` when `node[+0x1c0] & 1 == 0`, while `0x157b4` sets or clears exactly that bit on script-selected nodes. So the process is not “lookup OBJECT.MOB entry -> scene palette”; it is “current script/state names and classifies live entities -> item screen filters those live entities -> raw OBJECT.MOB icon blit happens under the palette already active for the current scene/state.”
- Important correction: `OBJECT.MOB` is not only feeding the 2x3 menu path. There is a separate live-sprite initialization path at `0x16d0c`, reached from object creation/setup callers at `0x167b2` and `0x1685e`. Those callers build a `0x240`-byte live node on the stack or in a working buffer, then call `0x16d0c`, then hand the initialized node to `0x16b10` for insertion into the live linked list rooted at `0x0bd2e8`.
- The upstream live-node constructor is now source-backed in more detail. The real body at `0x16728` clears a temporary `0x240`-byte node, populates it through `0x16bd7` unless a special mode `2` shortcut is active, stores the current object serial from `0x0bd2e4` into `node[+0x1cc]`, applies a special-case name check against the string at `0x0b6a14`, then calls `0x25a76`, `0x16d0c`, and finally `0x16b10` to append the fully initialized node into `DAT_000bd2e8`. If an optional companion pointer is supplied, the same constructor copies companion-derived fields into `node[+0x1d0/+0x1d4/+0x1d8]` before insertion.
- The critical mistake in the current extraction model sits one step above `0x16d0c`: the game does not build live nodes directly from a raw top-level `OBJECT.MOB` record. The helper at `0x16bd7` first calls `0x251f9`, which linearly searches a large runtime descriptor table rooted at `0x0dfb54` with count `0x0dfb50`. Each descriptor record is `0x3BC` bytes wide: `0x251f9` compares the incoming name against the record start, and `0x16bd7` then copies `0xB8` bytes from `descriptor + 0x20` into the node core. After that copy, `0x16bd7` walks up to `8` slot records at `descriptor + 0x0DC`, then additional `5`-entry groups at `+0x1DC`, `+0x27C`, and `+0x31C`, using `0x26c5d` and `0x269cd` to validate and merge those components into the node. So the runtime visual object is a descriptor-composed node, not a bare extractor decode of the top-level MOB entry.
- The source for that descriptor table is now recovered too. The setup routine at `0x251a4` opens `char.dat` in binary mode (`"rb"`, `"char.dat"` at `0x0b7580/0x0b7583`), calls `0x13d55` to read a `4`-byte little-endian count into `0x0dfb50`, allocates `count * 0x3BC` bytes through `0x11968`, stores the result in `0x0dfb54`, and then passes the file handle plus destination buffer to `0x59e9e`.
- `0x13d55` is not a seek/size helper; it is a straight `4`-byte read from the current file position. `0x59e9e` is the decompressor for the following payload: it first reads a `4`-byte decoded-size header, then expands a flag-driven LZ stream with a `0x1000`-byte ring buffer. Flag bit `1` copies one literal byte; flag bit `0` reads a `16`-bit pair, uses the upper `12` bits as the back-reference offset, the low `4` bits as `length - 3`, and copies `length = lowNibble + 3` bytes from the ring buffer. This is the same `0x1000` / `12+4` LZ family already used elsewhere in the engine.
- The shipped `Samples\CHAR.DAT` validates that loader path exactly. Its first dword is `295`, its second dword is `0x44DA4`, and `295 * 0x3BC == 0x44DA4`. The extractor-side `Scordato` probe now reproduces that path and emits `TestOutput\Scordato\CHAR\manifest.json`, `decoded.bin`, and one `record_###_<name>.bin` file per descriptor record. The decoded names are the expected live-character / monster descriptor set (`히로`, `인전남`, `인전여`, ...), which matches the `0x16bd7 -> 0x251f9 -> 0x16d0c` live-node model.
- That result also narrows the remaining problem for bad OBJECT renders like entry `371`. `OBJECT.MOB` entry `371` decodes to the top-level name `마돌보물찾음`, and that name does not appear exactly anywhere in the `295` decoded `CHAR.DAT` descriptor names. So `371` is not a simple raw-name bridge into this specific `char.dat` live-character descriptor table. The `char.dat` path is real and now parser-backed, but `371` still needs a different descriptor/object association path.
- `0x16d0c` is the first direct non-menu `OBJECT.MOB` load path we have for real sprites. It reads a type/index from `node[0]`, resolves a definition through the table at `0x0a59f4`, calls `0x13e0c` to materialize the definition tables, then calls `0x13fa8(node + 4, node + 0x224)`. `0x13fa8` writes two resolved object-data pointers into `node + 0x224` and `node + 0x228`. After freeing the temporary definition tables via `0x13ec9`, the initializer computes `node[0x220] = FUN_00013f84(node[0x228], 0x0B) + 0xA8`, i.e. it extracts subitem `0x0B` from the `node + 0x228` object bundle and caches the sprite payload pointer in `node + 0x220`. It then calls `0x17424(node[0x228])`, which looks up companion metadata through the table rooted at `0x0a59fc`.
- The archive-family selection in that live path is also narrower than the extractor has been assuming. `0x25a76` remaps `node[+0x0B8]` through the table at `0x0a7478` using `node[+0x28]` and `node[+0x38]`, and `0x16d0c` then indexes the archive-name table at `0x0a59f4`. The first two entries at that table are concrete strings: `main.mob` and `npc\npc.mob`. The next table at `0x0a59fc` is companion metadata, not more archive-name pointers. So the traced live-node sprite path we have today is not reading arbitrary top-level entries out of `OBJECT.MOB`; it is resolving named records inside `main.mob` / `npc\npc.mob`, then extracting subitem `0x0B` from those resolved bundles.
- This means the actual sprite path and the menu-icon path are using the same underlying `OBJECT.MOB` bundle member. The menu path at `0x2b66d` resolves `FUN_00013f84(node[0x228], 0x0B) + 0xA8` on demand each frame, while the live-node path caches the same pointer once in `node + 0x220` during initialization.
- This also sharpens the current user-visible failure on unresolved entries like `371`: the extractor is still rendering the raw top-level `OBJECT.MOB` payload as if that were the final runtime visual, but the traced game code shows a descriptor-resolution and slot-composition layer before live nodes ever reach `0x16d0c`/`0x2b66d`. Until that descriptor layer is recovered, a wrong-looking result for entries like `371` is no longer best explained as “missing palette only”; the higher-confidence root-cause candidate is that we are decoding the wrong runtime object representation.
- A separate live render path at `0x56426` / `0x56580` proves that cached sprite pointer is used outside the menu grid. `0x56426` rebuilds a visible-slot array `0x0f5e24` from the current linked list `0x0bd2e8`, selecting nodes whose `+0x1c4` field is zero. `0x565fb` then draws the real sprite from `node[0x220]` through `0x1105c(..., ECX = 0)`. If `node[0x1f0] >= 0x14`, it overlays shared grouped art from `FUN_0001b2ac(DAT_000ec9cc, 8, 1)` on top. This is a genuine non-menu sprite consumer of `OBJECT.MOB` data.
- Current source-backed palette conclusion should therefore be stated more narrowly and more accurately: both the menu-icon path and at least one live-sprite path use `OBJECT.MOB` sprite payloads derived from subitem `0x0B`, but none of the traced initializer or draw paths (`0x16d0c`, `0x2b66d`, `0x56426`, `0x56580`) upload or convert palettes. These consumers appear to render raw indexed sprite bytes under the already-live global palette state. That explains why some `OBJECT.MOB` outputs can look acceptable under the extractor's current palette logic while others still fail; the underlying sprite source is real, but palette authority remains external to the OBJECT bundle consumers we have traced.
- Manifest-backed correction from the latest `OBJECT.MOB --forga-scene D-2` validation: the current regression set the user called out (`74`, `371`, `405`, `412`, `461`, `481`) all decode with `LiveWorldSpritePaletteCandidates = []`, no `LiveWorldSpriteDataItemIndex`, and decoded data items marked `IsLikelyLiveWorldSprite = false`. These entries are therefore outside the traced `item 0x0B` live-world sprite path, so a scene-palette rerender on them is only a manual preview and not source-backed proof of correct palette ownership.
- `0x5d778` is plain memcpy with `dest = EAX`, `source = EDX`, and `count = EBX`. That resolved the direction of both the scene-header wrapper copy and the palette-buffer copies.
- `0x224cb` is a direct raw VGA DAC uploader: it waits on port `0x3da`, then writes `0x300` bytes from the caller buffer to ports `0x3c8` / `0x3c9`. The title-screen path at `0x5a500` loads `tit.raw`, reads `tit.pal` into `0x102ee0` through `0x5a804`, and then uploads it through `0x224cb`.
- `0x1afd4` is the named `.spb` header loader. It opens the requested archive, reads the full `0x408` header into `0x0c4c64`, builds the `36`-byte top-level entry table in `0x0c5070`, and optionally copies that full header to a caller buffer.
- `0x3351f` is a `diskres.spb` wrapper on top of that loader. It calls `0x1afd4` for `diskres.spb`, copies the `0x400`-byte tail at header offset `0x06` back to a caller buffer, then resolves the requested subresource through `0x1b0da` / `0x1b083`. The state machine at `0x17f99` allocates a local `0x400` buffer, calls `0x3351f`, and then immediately hands the returned pixel payload to `0x1105c`.
- The `diskres.spb` selector is now sample-backed too, not just disassembly-backed. The real body at `0x1b0da` treats `EAX` as a top-level `0x24`-byte entry index and `EDX` as the subordinate selector inside that entry; the outer controller at `0x17fee` passes the fixed tuple `(EAX = 1, EDX = 0, EBX = stackPaletteBuffer)`. On the shipped sample archive `DISKRES.SPB`, entry `1` decodes to `GAME_OVER`, which keeps this path firmly in menu/UI resource territory.
- Current concrete palette-path conclusion: gameplay-style SPB/MOB draws still blit raw indices, but the palette-bearing data traveling alongside them is the active scene/resource `.spb` header tail loaded by the named-header wrapper (for example `diskres.spb`), not the per-object `.mob` header. The exact gameplay DAC-upload call for that branch is still unresolved, but extractor-side palette selection should now follow the active scene/resource header rather than `OBJECT.MOB` or `BASEPAL.PAL`.
- Extractor behavior is now aligned with that narrower conclusion. The default no-palette `OBJECT.MOB` run still leaves unresolved entries on their embedded-header preview palette, but it now also emits a traced menu/resource candidate render for decoded non-live-world data items under `TestOutput\Scordato\OBJECT_transition_candidates\entry_*\menu_palette_candidates\DISKRES outer menu resource header\`. The candidate uses the `DISKRES.SPB` header RGBX palette written to `TestOutput\Scordato\DISKRES\header_palette.rgbx.bin`, and its manifest evidence string explicitly ties it back to `0x17f96 -> 0x33512(1,0) -> 0x14ac1`. This is still a candidate path, not proof that every unresolved OBJECT entry belongs to that exact menu state.
- The first concrete non-menu gameplay branch sits elsewhere: `0x18ee0` loads `static.dat` into globals `0x0c50d8` / `0x0c50d4`, and the world/object state machine at `0x1cbe4` later consumes that state. This separates the world/object path from the earlier `char.dat` and `diskres.spb` state machines.
- The world/object state machine at `0x1cbe4` is now partly source-backed, but the earlier `0x1ab52 -> 0x1b314` interpretation was a bad raw decode and should not be reused. In the corrected state-`3` block, when `0x0c50e0` is null the code loads `EAX = [0x0c5428]` and `EDX = [0x0c5430]`, calls `0x21325`, then calls `0x1e82a` and stores the result in `0x0c50e0`.
- The concrete `0x0c542c` consumers seen in the corrected decode are different from the earlier guess. Around `0x1cbd3`, the state machine checks `0x0c542c`; if it is null it calls `0x1ab48(0)` and stores the return value in `0x0c50e8`, and if both `0x0c542c` and `0x0c50e8` are non-null it calls `0x1cced(EAX = [0x0c50e8], EDX = [0x0c542c])` before continuing into `0x1dc34`.
- `0x1cced` is now partly decoded and it does not look palette-related. It walks a linked structure through field `+0x23c`, clears bit `0x04` at byte field `+0x1c0`, and rebases per-node coordinates at `+0x1d0` / `+0x1d4` using the active object's signed words at `+0x22` / `+0x24`. That makes the corrected `0x0c542c` branch a placement/list-adjustment path, not the missing gameplay palette authority.
- The earlier easy interpretation of `map\forga.fam` is still disproven at the file level: the live file on disk does not begin with the `0x408` archive header (`0x3176` at file offset `0`). More importantly, the previously claimed world-side `0x408` loader at `0x1ab52` was based on a mis-anchored byte stream and is now retracted. The exact world-side container or record behind the gameplay branch is still unresolved.
- `FORGA.FAM` is still useful, but it is not the earlier retracted `0x408` archive. The runtime loader at `0x1ad10` now makes the on-disk structure concrete: file magic `v1T`, first-table count `0x0123` (`291`) at file offset `0x0004`, first table `291 * 16` bytes from `0x0006..0x1235`, then a second header at `0x1236` with dword `0x00b85aa4` and second-table count `0x0069` (`105`), followed by a second `16`-byte table from `0x123C..0x18CB`.
- Both `FORGA.FAM` tables use the same runtime lookup shape: `char name[12] + u32 dataOffset`. The first table contains scene-like names (`AREA02`, `AREA27-1`, `JUNGLE`, `SEC1-1`, `GORAKS`, `WORLD`, `AREA34`), while the second table contains the shared resource names consumed by the same scene path (`AREA02`, `AREA04`, `AREA09`, `AREA13`, `AREA18`, `AREA19`, `AREA20`, `AREA25`, `AREA27`, `AREA27-1`, `JUNGLE1`, `sec1-1`, ...).
- `0x1af87` is the table-name lookup helper for both tables. It walks fixed `16`-byte records with `strcmp`, so the string-at-`+0x0` / offset-at-`+0xC` model is now runtime-backed rather than just sample-backed.
- `0x1ae15` is the concrete scene loader. It looks up a first-table record by the requested scene name, seeks to `dataOffset + 4`, reads a `12`-byte embedded name, two words into the scene state, `0x18` more header bytes, then peeks a dword and calls the shared decompressor. After that, it uses the embedded `12`-byte name to look up a second-table record and loads a second decompressed buffer from that table.
- First-table scene records are now partly source-backed: the leading dword at file payload offset `0x00` matches `payloadSpan - 0x2C`, the embedded name lives at `0x04..0x0F`, words at `0x10..0x17` are stable area dimensions/flags, bytes `0x18..0x2B` are additional header data, and the dword at `0x2C` is the decompressed output length for the first blob. Sample-backed examples stay consistent with the runtime arithmetic: `AREA02 -> 200 * 190 * 5 = 190000`, `AREA04 -> 200 * 160 * 5 = 160000`, `SEC1-1 -> 40 * 100 * 5 = 20000`.
- The first and second FAM blob types share the same decompression wrapper. `0x59c24` peeks a 4-byte dword and rewinds the file pointer, and `0x59e9e` is a 4 KB window LZ-style stream that begins with the decompressed-size dword. For first-table records, that LZ stream begins at payload offset `0x2C`. For second-table records, the file payload begins with `u32 storedSpan = payloadSpan - 4`, and the shared LZ stream begins at payload offset `0x04`.
- The scene-to-resource bridge inside `0x1ae15` is now concrete enough to guide reconstruction work: the first decompressed buffer size is `field10 * field12 * 5`, and the second decompressed buffer is split with runtime pointers at `size - 0x10400` and `size - 0x10000`. The real body behind `0x14ac1` (`0x14ac9`) consumes the trailing `0x400` bytes as RGBX palette quads, and the xref cluster ending at `0x115ba` consumes the final `0x10000` bytes as a `256 x 256` remap / translation table. The leading part of the second buffer still feeds the `64`-byte `8x8` tile bank; only the trailing `0x10400` bytes are confirmed non-tile tail data.
- The first decompressed `FORGA.FAM` scene buffer is now mostly source-backed. Inside `0x1ae15`, the loader does not split it into five flat byte planes. Instead it derives three scene-field pointers with loader arithmetic and later reorders them into runtime globals: `bb634 -> a7718`, `bb638 -> a7710`, `bb63c -> a7714`, `bb640 -> a771c`, `bb644 -> a55ae`. Concrete consumers now show their meanings:
	- `bb634` is a byte attribute plane. `0x11ccf` / `0x11e92` read low nibbles and compare them against values like `1`, `2`, and `3`.
	- `bb638` is a dense `u16` tile-index plane that feeds the main 8x8 scene draw path.
	- `bb63c` is a second `u16` index plane. `0x11e84` reads it, resolves `(index - 1) * 0x40 + bb640`, and queues records from that graphics bank.
	- `bb640` is the leading second-buffer tile bank, with `64` bytes per `8x8` tile.
	- `bb644` is still the scene remap table pointer used by the separate remap consumers.
- The runtime scene draw path is now concrete enough to explain how the map is assembled. `0x261a7` resets the active view state and seeds `0x26727`, which builds a ring-buffer tilemap. `0x26727` draws one `8x8` cell at a time: base tile from the `u16` plane, optional transparent overlay tile from the second `u16` plane, and optional per-pixel translation when attribute bit `0x80` is set. The view blitters `0x26560` and `0x2662d` then copy a `320 x 240` window out of that ring buffer, with `0x2662d` treating source byte `0xFF` as transparent.
- `0x1ae15` also trims the loaded scene height by `0x28` rows before the caller publishes it, so the runtime-visible scene height is `field12 - 40`, not the raw first-table height word.
- Sample-backed validation against `AREA02`: the loader-derived attribute plane contains only values `{0, 1, 3, 4}` and no cell sets bit `0x80`, so the unresolved translation-table branch is not needed for that sample's base map reconstruction.
- Corpus-backed follow-up over all `291` first-table `FORGA.FAM` scenes: no first-table scene sets attribute bit `0x80` anywhere in the published byte attribute plane. For this file, the unresolved `0xa55ae` / remap translation branch is therefore not a scene-family split; current scene differences are in the low nibble values instead.
- The low nibble value `1` is now source-backed as a blocking scene category. Both `0x11bc0` / `0x11c88` and the later world-side scratch-map builder at `0x2efc3..0x2f13f` read `bb634`, test for byte value `1`, and mark the corresponding occupancy bytes in `0xeb5cc`. The helper at `0x2f1cb` writes `2` across normalized object rectangles in that same scratch map, and `0x2f149..0x2f1c1` later outlines the active bounds with `3`, which makes the attr-`1` contribution part of the static blockage/collision mask rather than a draw-only flag.
- The low nibble value `3` is the source-backed foreground-mask threshold marker used by `0x11cfe..0x11efb`. That routine scans each visible scene column for the first attr-`3` cell, stores that row as the per-column threshold, then only queues second-plane tile records for rows at or below that threshold.
- The low nibble value `2` is the source-backed exclusion class inside that same foreground-mask builder. When `0x11e82..0x11ee0` visits a nonzero second-plane tile, it skips queueing if the current attribute low nibble is `2`, even when the overlay index itself is present. In sampled scene data, attr-`2` cells almost always carry nonzero second-plane indices, so current best reading is "foreground/occlusion hole" rather than "no overlay data".
- The queued scene records built by `0x11e82..0x11ee0` are now source-backed too. They increment the shared deferred blit count at `0xa0724`, write queue type `1` to `0xa0728`, store screen-space `x` / `y` at `0xa072c` / `0xa0730`, and store the resolved `8x8` tile pointer at `0xa0734`.
- `0x101de` is the shared deferred blit queue flush. For queue type `1` it calls `0x10dda`, which is an `8x8` transparent tile blitter that skips source byte `0xFF`. This makes the `0x11e60` scene path a deferred transparent foreground pass over the already-rendered work buffer rather than an alternate base-map decoder.
- The world-frame ordering around that queue is now source-backed. In the frame block at `0x1b554..0x1b59f`, the game calls `0x18ab0`, then `0x11476`, then `0x13099`, then `0x11462`. `0x18ab0` clears `0xa0724`, queues type-`0` sprite records for active world objects, and for the sorted middle object set it immediately calls `0x11cfe` after queueing each object record. `0x11476` is a thin wrapper over `0x263ab(0)`, which blits the base scene window into the work buffer. `0x13099` then draws additional direct `24x24` mode-`0` sprites with `0x1105c`. Finally `0x11462 -> 0x101de` flushes the deferred queue. This is the direct proof that the scene's attr-`3` / attr-`2` foreground tiles are appended after the relevant object records and therefore draw over actors / objects rather than under them.
- Attr-`4` is now best treated as a scene-terrain category rather than a gameplay branch. The remaining world/object-side check at `0x18c76 -> 0x11f05 -> 0x26259` only performs scroll/ring-buffer maintenance and never inspects the scene attribute plane, so there is still no surviving traced gameplay consumer for low nibble `4`.
- Sample-backed attr-`4` shape across the current corpus: `159` first-table scenes use it, it is always backed by a nonzero base tile (`12706` total cells = `6301` base-only + `6405` base+overlay, `0` overlay-only), and it overwhelmingly neighbors other attr-`4` cells vertically while meeting attr-`1` laterally at region edges. In practice this makes attr-`4` the bulk terrain/platform surface class, not a second foreground threshold and not a collision gate.
- Low nibble values `5` and `6` do occur in a small number of first-table scenes (`ASE-2`, `KHETCHA`, `KHETCHA2`, `TOW-2`, `D-E3`, `ALEKLIST`), but no traced direct consumer currently distinguishes them from the generic non-`2` / non-`1` path.
- Follow-up sweep for attr values `4`, `5`, and `6`: no direct compare against those low-nibble values was found in the current `bb634` consumer set. The heaviest current `5` / `6` users are `RAYBEOL`, `JH1`, `KHETCHA`, `KHETCHA2`, `D-E3`, and `TOW-2`, and those cells usually still carry second-plane tiles.
- Sample-backed relationship between `4`, `5`, and `6`: `5` / `6` behave like rarer decorated variants of the same terrain family as `4`, not as a separate runtime class. They are overwhelmingly self-contiguous, mostly base+overlay rather than base-only (`5`: `72` base-only + `236` base+overlay, `6`: `59` base-only + `189` base+overlay), and in the scenes that use them they border attr `0` / `1` / `4` cells rather than any unique gameplay-specific class.
- Current probe coverage for the remaining `5` / `6` families is now preserved under `TestOutput\Scordato\FORGA_probe_samples`: `RAYBEOL`, `JH1`, `KHETCHA`, `KHETCHA2`, `D-E3`, and `TOW-2` all have archived `scene_composite.png` plus attribute/base/overlay maps for side-by-side inspection.
- `Scordato` now mirrors the runtime-backed two-table model for `.fam` files. Running it on `Samples\MAP\FORGA.FAM` writes `manifest.json`, `first_table\chunk_*.bin`, and `second_table\blob_*.bin` under `TestOutput\Scordato\FORGA`, and the manifest now exposes the two table counts, the shared LZ metadata, the first-buffer `field10 * field12 * 5` check, and the second-buffer `size - 0x10400` / `size - 0x10000` tail split.
- The second `FORGA.FAM` table does not have unique names once names are trimmed, but the runtime `strcmp` does distinguish space-before-NUL variants. Concrete raw-name split: second-table entry `65` is `"pec-in \0"`, entry `66` is `"pec-in\0"`, entry `67` is `"ray-in\0"`, and entry `68` is `"ray-in \0"`. First-table embedded names also use both forms, so probe/resource lookup must preserve ASCII exactly up to the first NUL and must not trim spaces.
- `Scordato --probe-scene <name> Samples\MAP\FORGA.FAM` now follows the loader-backed layout rather than the falsified flat-byte split. It emits attribute, base-tile, and overlay-tile maps plus a source-backed `scene_composite.png` for samples that do not require the unresolved attribute-bit translation step. The old five-flat-plane output should be considered retracted.
- `Scordato --probe-scene` now also preserves the per-entry `.decoded.bin` outputs and `DecompressedOutputFile` manifest fields for FAM entries. This keeps later scene-classification and cross-scene analysis working even after a probe-only run.
- Later room-scene regressions (`D-9`, `D-2`, `C-1B`) were not caused by a lookup-table tile-id remap. The root cause was FAM name normalization in `Scordato`: trimming spaces collapsed distinct embedded/resource names that the runtime `strcmp` keeps separate. Concrete fixes from raw names: `D-2` and `D-9` embed `"pec-in\0"` and must bind second-table entry `66`, while `C-1B` embeds `"ray-in \0"` and must bind entry `68`. The earlier "remap out-of-range tile ids through the lookup table" explanation is retracted.
- A separate room-scene probe fix remains valid: the base scene layer was incorrectly treating tile pixel value `0xFF` as transparent. The traced transparent handling only applies to overlay / queued foreground draws; the base scene pass should emit palette entry `255` as an ordinary color. Sample-backed confirmation from `pec-in` / `ray-in`: palette entry `255` is `(255,227,183)`, not an empty key color, and the affected scenes use thousands of `0xFF` pixels inside base-layer tiles.
- After preserving exact FAM name spelling and keeping base-layer `0xFF` opaque, regenerated probes for `D-2`, `D-9`, and `C-1B` bind the runtime-correct second-table entries (`66`, `66`, and `68`) and produce plausible room interiors again.
- Validation status of the current scene path: a batch `Scordato` probe over all `291` first-table `FORGA.FAM` scenes completed successfully with `0` `ProbeError` entries, `0` missing probe directories, and `0` missing `scene_composite.png` outputs. Current source-backed conclusion: the present loader-derived base/overlay scene compositor can process the full first-table scene set in `FORGA.FAM`.
- The visually odd slice around `259_SAN1` through `267_SKY3` is not currently supported as a wrong-palette bug. Those entries are a distinct first-table family: no attr usage, no second-plane usage, and a fully dense single `u16` base plane.
- That family also groups cleanly by shared second-table resources and regular dimensions: `SAN1/2/3 -> SAN2`, `SANSKY1/2 -> SAN22`, `SANSKY3 -> SAN33`, `SKY1/2/3 -> SKY`, and the adjacent `SANSK1/2/3 -> SAN3`, all in the same `240x180`, `280x210`, `80x90` runtime size pattern.
- Palette check for that family: the current outputs are using the resource-local runtime palettes already consumed by the traced loader path, and the dominant repeated tiles in those scenes resolve to the same flat blue/green/olive colors seen in the PNG outputs. Example: `SKY1`'s dominant tile `1002` is a single palette index `53 -> (172,192,216)`, and `SAN1`'s dominant tile `2236` is a single palette index `41 -> (64,84,8)`. Current conclusion: if these scenes are semantically wrong, the remaining issue is their scene/layout role, not palette selection.
- Best current reading is that this `SAN*` / `SKY*` / `SANSK*` cluster is a special single-plane backdrop/panorama family rather than the normal attr/base/overlay walkable-scene family. That interpretation is sample-backed from the data shape, but the exact in-game use still needs a caller-side trace before it should be treated as fully source-backed.
- Practical status after this trace pass: the title path is still the only proven direct DAC upload (`tit.pal -> 0x5a804 -> 0x224cb`). The corrected `static.dat`-driven world/object branch is real, but the newly decoded `0x1cced` helper shows that one major `0x0c542c` path is only doing placement/list adjustment, so the missing gameplay palette authority is still elsewhere.
- Follow-up tracing closes that higher-level ordering. `0x17d46` is a top-level controller keyed by `0x0a5acc`, and the script interpreter itself writes that same state variable at `0x1a742`, `0x1a751`, `0x1a760`, `0x1a76f`, and `0x1a7f2` (states `8`, `0xc`, `0xa`, `0xf`, and `9`). Its subordinate controller at `0x17f8c` dispatches on `0x0a5ac0` and includes the full-screen blit + full palette upload path at `0x18000..0x18010`.
- The later substate at `0x18080` shows that this outer controller owns palette-adjacent scene transitions rather than per-object renderers. It calls `0x19123` (clear `0x0a5b3c`, `0x0c4874`, and `0x0c4870`), frees the live linked list via `0x16aa4`, frees two other object/resource lists via `0x17664` and `0x1d5f5`, resets screen state, then returns to controller states `0x0a5ac0 = 7` and `0x0a5acc = 3`.
- The actual gameplay/world tick is now source-backed at `0x1b46a`. It first calls `0x1c8d0`, then, when `0x0c50f4 == 0`, calls the scene script interpreter `0x19594`. Only after that does it gate on `0x0a5acc == 4` and dispatch the world substates through the `0x0c50f4` switch (`0x1c2f8`, `0x1ba24`, `0x1baf4`, `0x1bd3a`, `0x1bee4`, `0x1c9e0`, `0x1c058`, `0x1cc10`). This is the first ordered proof that scene script execution and the live world/object state machines share the same gameplay loop.
- No palette upload calls appear in the traced gameplay tick (`0x1b46a`), the script interpreter (`0x19594`), the `0x0c50f4` world substates, or the traced `OBJECT.MOB` consumers (`0x16d0c`, `0x2b66d`, `0x56426`, `0x56580`, `FUN_00030693`). Current source-backed palette model is therefore: palette changes happen in outer controller/resource states before `0x0a5acc == 4` gameplay resumes, and live `OBJECT.MOB` sprites inherit that already-live scene palette.
- Post-wrapper disassembly now makes that outer-owner model more concrete. The real gameplay tick body at `0x1b474` calls the real interpreter body at `0x19594` when `0x0c50f4 == 0`, and when `0x0a5acc == 4` it dispatches the active world substate through the `0x0c50f4` switch. The real bodies behind `0x1bd3a` (`0x1bd44`) and `0x1bee4` (`0x1beee`) both manipulate current live-node pointers (`0x0c5414` / `0x0c541c`), cached coordinates, string/resource helpers, and state variables (`0x0a5b98` / `0x0a5b9c`), but neither one calls `0x14a88`, `0x14ac1`, or any other palette uploader. So even the outer world substates immediately above the item/event path still inherit rather than install palette state.
- The nearest source-backed palette owner above that whole chain is therefore still the top-level resource controller, and the post-wrapper body at `0x17f96` now identifies it more concretely. In the branch at `0x17fe5..0x1802b`, the code allocates a local `0x400` palette buffer on the stack, calls `0x33512` (the verified `diskres.spb` fetch wrapper), blits the returned payload through `0x1105c`, then calls `0x14ac1(EAX = 0, EDX = 0xFF, ECX = 0, EBX = stackPaletteBuffer)` at `0x18010` to convert and upload the full external palette before advancing `0x0a5ac0` to `0x0A`. Combined with the absence of any later palette upload in `0x1b474`, `0x1bd44`, `0x1beee`, `0x2a87e`, and `0x2ada6`, the current strongest conclusion is: unresolved menu/event entries in this caller chain render under the already-live global resource palette last installed by the outer controller, not under any `OBJECT.MOB` entry-local palette.
- Important scoping note for that palette conclusion: the other full upload we surfaced nearby, `0x2a3a5 -> 0x14a88`, belongs to a different controller path around `0x0f2860` / `0x0e7368`, not to the `0x19be7 -> 0x2a874 -> 0x2ada6` item-screen chain. It should not be treated as the owner for this menu/event path unless a later shared caller is proven.
- The immediate handoff into gameplay is now narrower too. In the outer `0x0a5acc` controller, state `3` is the branch at `0x17db7`: it calls `0x56df7`, and when that helper returns zero it writes `0x0a5acc = 4` and falls back through the common per-frame tail. `0x56df7` does not upload palettes; it draws shared art from `DAT_000ec9cc` via `FUN_0001b2ac(DAT_000ec9cc, 0x0A, 0)`, blits it with `0x1105c`, copies the work buffer back to the screen, and then polls input/state through `0x1008e`, `0x56ce6`, and `0x56d7e`. Current reading: this is a pre-gameplay gate/overlay state, not a hidden palette-install step.
- The indexed OBJECT interaction routine `FUN_00030693` is now recovered as a real function, with a concrete caller at `0x32f06`. That caller loads `EDX = [0x0eb88c]` and `EAX = [0x0eb740]` from a separate raw controller keyed by `0x0ec9c8`; inside that controller, case `5` dispatches to `FUN_00030693` after selecting the active target from the table at `0x0eb898`. This places the `node + 0x220`-style indexed OBJECT interaction code in another world-side controller, but still without any local palette authority.
- The sibling interaction path above `FUN_00030693` is now source-backed too. The shared-target controller `FUN_00027e3c` (entry `0x27e3c`, state key `0x0e43d0`) is called from `0x32ed4`, which sits in the same `0x0ec9c8` controller as the later `0x32f06 -> FUN_00030693` call. Inside that controller, the `0xf2860` target-state branch and the indexed `node + 0x220` OBJECT branch are therefore two states of one runtime subsystem, not independent consumers. `FUN_00027e3c` itself dispatches on `0x0e43d0` and drives the shared target pointer `0x0f2860`; the later `0x32bxx` states feed it through `0x0eb884` / `0x0eb888` and then advance to the `FUN_00030693` branch through `0x0eb88c`.
- The internal `0x0ec9c8` state flow is now concrete as well. Jump table `0x32af0` maps states `0..6` to `0x32b32`, `0x32b6b`, `0x32e29`, `0x32ec9`, `0x32ee9`, `0x32efb`, and `0x32f0d`. State `0` probes the current object at `0x0eb740` through `0x27ce5` and sets a countdown at `0x0eb87c = object[0x74] + 5`, then advances to state `1` or `6`. State `1` calls `0x32f21`, walks the candidate table at `0x0eb898`, stores the chosen candidate in `0x0eb88c`, and then chooses one of three helper branches: `0x0eb884` for the shared-target path (`state 3 -> FUN_00027e3c`), `0x0eb888` for the alternate helper path (`state 4 -> 0x292b0`), or direct `0x0eb88c` handling (`state 5 -> FUN_00030693`). State `2` is a wait/step gate around `0x0dfab0/0x0dfab4/0x0dfab8` plus the countdown in `0x0eb87c`; it updates object field `+0x1c4`, calls `0x2f98c` / `0x2f8ce`, and then re-enters the chosen follow-up state. State `6` calls `0x2f642` and resets `0x0ec9c8` to `0`.
- There is now a clearer, source-backed outer owner above those same interaction branches even though the direct caller of `0x32b0c` itself is still hidden. A separate controller at `0x31d98`, keyed by `0x0eb868`, dispatches `13` states through jump table `0x31d64`: `0 -> 0x31e04`, `1 -> 0x31fbd`, `2 -> 0x3202a`, `3 -> 0x32076`, `4 -> 0x1ee24`, `5 -> 0x32185`, `6 -> 0x3258e`, `7 -> 0x32260`, `8 -> 0x3296b`, `9 -> 0x329de`, `10 -> 0x32818`, `11 -> 0x32356`, `12 -> 0x32471`. This controller always exits through the common `EAX = [0x0eb740] ; jmp 0x2f7a0` tail.
- The important late `0x0eb868` states are now pinned down exactly. State `5` (`0x32185`) directly calls `FUN_00030693(EAX = [0x0eb740], EDX = [0x0f2860])`. State `11` (`0x32356`) directly calls `FUN_00027e3c(EAX = [0x0eb740], EDX = [0x0c936c])`. State `12` (`0x32471`) directly calls `0x292b0(EAX = [0x0eb740], EDX = [0x0c936c])`. That proves the indexed OBJECT interaction path and the two sibling helper paths are not isolated islands: they are explicit late states of this outer `0x0eb868` object/world controller.
- The selection path into those late states is also source-backed now. State `4` of the `0x0eb868` controller calls `FUN_0001ee24`. That routine is a separate selector/state machine keyed by `0x0c9364`; after its animation/countdown path completes, it returns `EAX = [0x0c9368]` at `0x1f239`. The table at `0x0a8ecc` then maps those return indices to the outer controller states: `0 -> 5`, `1 -> 6`, `2 -> 7`, `3 -> 8`, `4 -> 9`, `5 -> 10`, `6 -> 11`, `7 -> 12`, `8 -> 3`, `9 -> 15`. So the `0x0eb868` controller is explicitly routing selector results into the direct `FUN_00030693`, `FUN_00027e3c`, and `0x292b0` branches.
- `DAT_000eb740` is now also confirmed as a live "current object" pointer in a higher-level object loop, not just a local scratch global inside the interaction controllers. In the separate controller band around `0x2e250`, the code calls `0x2f6dc`, stores the returned node to `DAT_000eb740`, initializes some object fields, and then calls `0x2f7a0`. The same `0x2f7a0` routine is the common tail that the `0x0eb868` controller jumps to after its state handlers. This does not yet prove a direct call edge into `0x31d98`, but it does prove that both paths share the same live-object model and post-state object finalizer.
- Palette authority for correct `OBJECT.MOB` rendering is now strong enough to treat as definitive for the `FORGA.FAM` gameplay scenes. A corpus comparison over all `105` second-table `FORGA` tail palettes shows exactly one pattern across the full set: indices `0..127` are scene-specific and vary per resource, indices `128..254` are stable across all `105` palettes except for a fixed patched set `{153, 155..158, 175..183, 242}`, and there is no scene-to-scene variation anywhere in the high range. In other words, the live gameplay palette is not the `OBJECT.MOB` / `BASEPAL.PAL` header palette; it is the current scene palette, with a scene-varying low half and a mostly fixed high half.
- That finally explains the existing OBJECT export split without assumptions. Low-index entries such as `74`, `371`, `405`, `412`, `461`, and `481` are wrong under the archive/base palette because they need the current scene's low-half palette. High-only entries can look acceptable under `BASEPAL.PAL` only when they stay inside the unchanged high range, but that is an accident of index usage, not evidence that `OBJECT.MOB` carries its own runtime palette. Entries that touch the fixed high-range patches (`153`, `155..158`, `175..183`, `242`) also need the live gameplay palette rather than raw `BASEPAL.PAL`.
- Practical rendering rule for `OBJECT.MOB` is now: decode the sprite payload exactly as the existing RLE path does, keep the decoded palette indices unchanged, and apply the active `256`-entry gameplay palette. For `FORGA.FAM` scenes, that gameplay palette is the `0x400`-byte `tail-palette.rgbx.bin` extracted from the active second-table resource. There is no per-entry palette inside `OBJECT.MOB`, and the runtime does not remap sprite indices locally before drawing.
- Index `255` also matters here: raw `256 x 4` scene palettes have a real opaque entry `255`, so offline rendering must not silently force palette index `255` to transparent when using those runtime tail palettes. Transparency in `OBJECT.MOB` comes from the row opcodes / alpha mask in the sprite payload, not from palette index `255`.
- `Scordato` now accepts raw `255 x 4` and `256 x 4` RGBX palette files through `--palette`, including the extracted `FORGA` `*.tail-palette.rgbx.bin` files. That makes the runtime-correct workflow concrete: choose the scene/resource palette that was active when the object is displayed and feed that palette file directly to the extractor instead of using `OBJECT.MOB` or `BASEPAL.PAL`.
- `Scordato` now also accepts `--forga-scene <scene-or-resource-name>` (plus aliases `--palette-scene` / `--scene-palette`) as the lower-friction path for `OBJECT.MOB` rerenders. If `--palette` is not supplied, the tool resolves the matching `FORGA` second-table `*.tail-palette.rgbx.bin` through `TestOutput\Scordato\FORGA\manifest.json`; when that extraction is missing or lacks runtime palette outputs, it regenerates `TestOutput\Scordato\FORGA` from `Samples\MAP\FORGA.FAM` and then uses the resolved tail palette automatically.
- The new resolver is grounded on the exact `FORGA` manifest data rather than filename guessing. It first prefers a first-table scene match, then maps through that entry's embedded/probe resource name to the second table, so it handles the known trailing-space cases such as scene `D-1` resolving to resource `pec-in ` without requiring the user to type or even notice the space-before-NUL distinction.
- Object-to-scene association is now source-backed at the scene-transition level. Script helper `0x1ac91` reads `3` consecutive NUL-terminated strings from the current script stream into `0x0c4878`, `0x0c487c`, and `0x0c4880`, and opcode path `0x1a99a..0x1a9ad` copies that triplet into the persistent transition globals `0x0c5428`, `0x0c542c`, and `0x0c5430` through `0x1cbcd`. The same three-string mechanism also appears in the hardcoded startup path at `0x17f0f..0x17f1e`, which calls the sibling wrapper `0x1b369` with the CP949 strings `"고락스"`, `"마을"`, and `"마돌에서"`.
- Follow-up correction on that script side: `0x1ac91` is only a string-pointer snapshotter over the script cursor at `0x0c4874`, and `0x1cbcd` is only a staging helper that writes `EAX/EDX/EBX` into `0x0c5428/0x0c542c/0x0c5430` and sets `0x0c50f4 = 0x0D`. Neither helper derives filenames, selectors, or table blobs.
- The wrapper itself is now decoded enough to name the triplet roles. `0x1b340/0x1b369` clear previous state, pass the first string to `0x1350e`, pass the second string to `0x135fa`, and pass only the third string to `0x1e758/0x1e7b7`. So the triplet is not `3` peer names; it is a nested `top-level group -> subscene -> anchor` selector.
- The first-string / second-string resolvers do not lazy-load those tables themselves. The nearby helpers `0x17664/0x176d4` and `0x1d5f5/0x1d665` only free cache arrays, and `0x11af0` only resets scratch/cache blocks through `0x1af4a`. After that cleanup, `0x1350e` immediately consumes the already-published top-level table at `0x0bb85c / 0x0bb860`, and `0x135fa` immediately consumes the already-published nested table at `0x0bb864 / 0x0bb868`.
- Alignment correction: the real local entrypoints for the raw transition loaders are earlier than the labels first recovered from the partial block walk. `0x133f8` is the `saga.scp` loader entry, `0x13455` is the `saga.bin` loader entry, and `0x1349f` is the local parse entry that consumes the buffer published through `0x0bb850`.
- Correction on the transition-file decode side: `saga.scp` and `saga.bin` are both top-level `0x59e9e` streams with a leading decompressed-size dword, not a separate `0x59e8c` family. Sample-backed check: `Samples\SAGA.SCP` expands from `0x001884A9` bytes and `Samples\SAGA.BIN` expands from `0x000FF8AA` bytes under the same existing `12+4` ring-buffer decoder already validated for `CHAR.DAT` / `FORGA.FAM`.
- `0x133f8(EAX = filename)` supplies the local mode string `"rb"` from `0x0b6988` into `0x13c90`, loads a raw file into `0x0bb84c`, and publishes `0x0bb86c / 0x0bb870` as a `count + 0x24`-byte record table. `0x13870(index)` returns `0x0bb84c + entry[+0x20] + 4`, while `0x13955` and `0x139ac` scan those same `0x24`-byte records and return that same blob pointer when their per-record predicate helpers (`0x5cdbf` and `0x5ce10`) accept the record. These are not plain string compares.
- `0x13455(EAX = filename)` supplies `"rb"` from `0x0b698b`, loads a raw file into `0x0bb850`, and its local parse entry `0x1349f` skips a `count * 0x80` section and then a `count * 0x44` section before publishing a word/pointer pair at `0x0bb85c / 0x0bb860`. The word at `0x0bb85c` is a 16-bit selector extracted after those counted sections, not another `0x24`-byte name table.
- `0x1350e` no longer supports the earlier “first string is looked up directly in `0x0bb85c / 0x0bb860`” reading. After only checking that its input string is non-null/nonempty, it clears caches, feeds the selector pair `0x0bb85c / 0x0bb860` into `0x13955`, stores the returned first-table blob pointer in `0x0bb854`, and parses `record + 0x20` as counted sections of sizes `0x44`, `0x24`, `0x30`, `0x08`, and `0x04`. The new direct bridge is now source-backed too: the fifth counted `0x04` section is passed straight to `0x1d5b5(EAX = count, EDX = sectionBase)`, so the first string / scene-group selection is the concrete upstream source of the selector-to-`OBJECT.MOB`-entry alias table at `0x0c543c / 0x0c5440 / 0x0c5444`. Only after that does `0x1350e` publish the final word/pointer pair as `0x0bb864 / 0x0bb868`.
- `0x135fa` behaves the same one level deeper. After the same non-null/nonempty gate, it feeds `0x0bb864 / 0x0bb868` into `0x13955`, stores the returned blob pointer in `0x0bb858`, keeps `0x0c50f8 = record + 0x20`, runs `0x11994` over the fixed block at `record + 0x20 + 0xA2`, and then walks another series of counted sections. The critical one is now pinned down: the later counted `0x44` section is passed directly to `0x1d445(EAX = count, EDX = sectionBase)`, which allocates `0x0c5434 / 0x0c5438` and builds the live world-object table from `0x44`-byte scene-object source records. Only after that does the final word/pointer pair go through `0x1e724`, which publishes the active `0x2A` scene-record table.
- Corrected transition-stage conclusion: the wrapper still stages `first-string path -> second-string path -> anchor`, but `0x1350e / 0x135fa` themselves are selector-driven table consumers, not direct name lookups. The normal runtime filename bridge is no longer missing: at `0x17b58`, the game calls `0x133f8("saga.scp")`, then `0x13455("saga.bin")`, then `0x1349f()`. That is the concrete upstream path that seeds the selector pair consumed later by `0x1350e / 0x135fa`.
- This closes the previously missing control bridge for the direct top-level `OBJECT.MOB` world-object path. The first transition key selects the alias table that maps per-object selectors to real top-level `OBJECT.MOB` entry indices, the second transition key selects the `0x44`-byte scene-object records that carry those selectors and placement fields, and the third key is still only the later anchor/entry-point lookup. Unresolved entries like `371` therefore belong to the first-two-key scene/subscene pipeline, not to `CHAR.DAT` and not to a fixed-name `OBJECT.MOB` branch.
- There is also a separate savegame restore bridge. `0x2c0a4` takes a save-slot index in `EAX`, uses it to index the filename table at `0x0a84d8` (`"saga00.sav" .. "saga04.sav"`), opens that save file with mode `"rb"`, reads a `0x14c` block and then a `0x444` block, calls `0x2c572` to rebuild the live object list from save contents, calls `0x2c661` to read the remaining save-file tail into `0x0bb850`, then calls `0x1349f()`, `0x1350e(0x0eaed4)`, and `0x135fa(0x0eaef4)`. So the save/restore path bypasses the raw-file loader wrappers and reconstructs the same selector/blob state from the save file instead.
- The current world/object state `3` at `0x1ccaa` still consumes that triplet via `0x1b340(EAX = [0x0c5428], EDX = [0x0c542c], EBX = [0x0c5430])`, but the refined reading is now: the second string's selected record publishes the active lookup table at `0x0c9358 / 0x0c935c`, and the third string is only the named anchor looked up inside that active `42`-byte table. `0x1e73a` exposes the count/base globals, and `0x1e837` walks that table by name.
- There is now a second staged-key entry into that same downstream wrapper. In the sibling path at `0x1c12b..0x1c138`, the code loads `EDX = [0x0c5424]`, `EAX = [0x0c5420]`, forces `EBX = 0`, and then calls `0x1b340` before the filtered/sorted node-to-scene binding walk. The writers to `0x0c5420 / 0x0c5424` are still unresolved, and this two-value path is currently the best remaining lead on the hidden upstream bridge.
- `0x1c058` is now constrained as a separate top-level world/object mode, not just a stray call site. It is keyed by `0x0a5ba0 = 0x11..0x15`; its opening path `0x1c0cc..0x1c138` performs setup and then enters `0x1b340(EAX = [0x0c5420], EDX = [0x0c5424], EBX = 0)`. After that, `0x1e73a` exposes the active `0x2A`-byte scene-record table, the code filters the live node chain to `node[+0x1c4] == 0`, sorts those nodes by `node[+0x1cc]`, and binds them through `0x22c46(node, sceneRecord)`. Current source-backed reading: this is the room-level / no-anchor object-scene binding variant.
- `0x1cc10` is the anchored sibling of that room-level path. In state `3` at `0x1ccaa..0x1ccbb`, it consumes the staged triplet `0x0c5428 / 0x0c542c / 0x0c5430`, calls `0x1b340`, resolves the named anchor with `0x1e837([0x0c5430])`, and then `0x1ccf7..0x1cd55` rebases live node coordinates `+0x1d0/+0x1d4` relative to the anchor record's signed `+0x22/+0x24`. This is placement/entry-point logic, not a separate palette path.
- Local feeder checks are now strong enough to bound the remaining unknown. The nearby object/world modes at `0x1ba30`, `0x1bb00`, `0x1bd46`, `0x1bef0`, and `0x1c304` do not stage `0x0c5420 / 0x0c5424`, and no direct writer to that pair has surfaced in the local object-state cluster. Current tightest source-backed conclusion: the sibling pair is externally staged global state parallel to the known three-string triplet path, likely from a broader world/location setup path rather than from a local helper next to `0x1c058`.
- The remaining unresolved upstream question is now narrower: the raw filename bridge itself is solved by `saga.scp` / `saga.bin`, and the downstream object-to-scene semantics of the two-key versus three-key paths are now clear, but the concrete writers to the sibling staged-key pair `0x0c5420 / 0x0c5424` are still hidden.
- Direct sample-file inspection now adds one extractor-safe bridge between the transition data and FORGA palette selection. Exact uppercase token scans over the CP949-decoded `Samples/SAGA.BIN` and `Samples/SAGA.SCP` surfaces `22` standalone strings that exactly match FORGA first-table scene names, including `D-A1`, `D-B4`, `D-D2`, `D-D4`, and `N-2`. That is strong enough to emit transition-backed scene palette candidates and live-world sprite colour variants in the extractor, but it is still not a per-object palette proof because the current `OBJECT.MOB` extraction does not expose a source-backed entry-local scene key.
- Additional negative result: `0x137b0` and its caller `0x1b3e9` are downstream-only consumers of the already-published selector pair `0x0bb85c / 0x0bb860`. They query the currently selected transition blob and return a boolean/status value; they do not choose filenames or publish selectors.
- `0x1e758` and `0x1e7b7` prove what those lookup records mean. After `0x1e837` returns a matching record, both helpers derive scene tile coordinates from record fields around `+0x22/+0x24/+0x26` and feed them into the scene helpers at `0x11b0e` / `0x15bdf`. That is strong evidence that the triplet's third string is a named scene anchor / entry-point inside the already selected subscene, not an object palette tag.
- The live object list is then rebased against that selected scene anchor. In `0x1cccf..0x1cd55`, after `0x0c50e0` is chosen, the engine adjusts the anchor bounds and walks the live node chain rooted at `0x0bd2e8`, clearing node flag bit `0x04` and rebasing node coordinates `+0x1d0/+0x1d4` relative to the selected anchor record. This is the first concrete runtime path showing how scene selection feeds object placement.
- Immediate follow-up after the second-string load is now narrower too. `0x1b392` only inspects the fixed header at `0x0c50f8` and conditionally kicks the `0x24d34/0x24fa7` state-machine path; it does not itself resolve a per-object palette or private scene tag. So the strongest current palette model remains: palette authority follows the active subscene selected by the first two transition-key strings, not the third-string anchor and not any `OBJECT.MOB` entry-local tag.
- The nearest surviving branch that still carries the raw second-string pointer is `0x19792 -> 0x1b856 -> 0x12df9`, but that path now looks like overlay/UI state rather than a scene-resource bridge. `0x12df9` copies the raw second string into working buffer `0x0bb6d0` (optionally combining it through `0x1df30`), computes screen placement into `0x0bb6bc/0x0bb6c0/0x0bb6c4/0x0bb6c8`, and arms state `0x0bb6b0 = 1` plus `0x0a59c4 = 0x0A`; `0x12f4f` then renders through `0x1105c`. So this branch preserves the subscene name for display/state purposes, but still does not reveal a FORGA first-table or second-table identifier.
- The next scene/object binding stage is now also source-backed. In `0x1c138..0x1c1f7`, after `0x1b340` selects the current lookup table, `0x1e73a` returns its count/base (`0x0c9358` / `0x0c935c`). The code then filters the live node chain to nodes with `node[+0x1c4] == 0`, sorts those node pointers with comparator `0x15b82`, and walks them in lockstep with the active `0x2A`-byte scene records. The comparator is simple and concrete: `0x15b82` sorts by `node[+0x1cc]`.
- Per-node scene binding happens at `0x1c1d4 -> 0x22c46(node, sceneRecord)`. That helper resolves the node's attached sprite/object data through `0x2322b(node + 4)`, uses the scene record coordinates (again through the signed words around `+0x22/+0x24/+0x26`), and writes scene-relative placement into the live node at `+0x2c/+0x30/+0x34` before calling `0x22bb3`. This is the strongest direct evidence so far that the game associates live OBJECT sprites with a scene by pairing scene records to already-live nodes, not by looking up a palette or scene tag from `OBJECT.MOB` itself.
- The sort key ties back to earlier script findings. `0x157b4` updates matched live nodes at `+0x1c4/+0x1cc`, and the later scene binder uses exactly `+0x1cc` for ordering and `+0x1c4 == 0` as its active filter. So at least part of the object-to-scene association is mediated by script/state fields on the live node, then finalized against the current scene record table during the world-state transition.
- Current strongest statement of the binding rule: script opcodes name live nodes by string, assign an integer-like scene/state category into `node[+0x1c4]` through `0x0c48a0`, assign an order/index into `node[+0x1cc]`, and then the world transition code binds exactly the `+0x1c4 == 0` subset to the active scene records selected by the current three-string transition key.
- Current best conclusion: the game does not appear to attach a private scene/palette tag to each `OBJECT.MOB` sprite. Instead, scene association is carried by the active three-string transition key (`scene-group` / `subscene` / `entry-name`), which selects the current scene lookup table and entry anchor. Live `OBJECT.MOB` sprites then inherit whatever palette that active scene transition has already installed.
- The last unresolved point in that chain is now explicit rather than vague: the outer caller of the `0x0ec9c8` controller still has not surfaced as a normal direct xref in Ghidra, but the internal ownership is clear. We now have `0x32ed4 -> FUN_00027e3c` and `0x32f06 -> FUN_00030693` inside the same controller, while gameplay itself only resumes after the outer `0x0a5acc` controller leaves the non-palette gate at `0x17db7 -> 0x56df7` and enters `0x0a5acc == 4`.
- Sampled `.mob` files do not use the `.spb` 36-byte top-level entry table. `MAIN.MOB` and `MISSILE.MOB` both parse cleanly as 40-byte top-level records:
	- `char name[32]`
	- `u32 metadataOffset`
	- `u32 dataOffset`
- With that 40-byte top table, top-level MOB names decode cleanly in CP949. Examples from `MISSILE.MOB`: `피라에로우`, `피라핸드`, `피라대저`, `피라배리어`, `피라볼`. Examples from `MAIN.MOB`: `인전여`, `인전남`, `히로`, `인기남`.
- For sampled `.mob` entries, `metadataOffset` points to a self-describing metadata region with:
	- `u32 outerSize`
	- `u16 recordCount`
	- `recordCount` records of `char name[32] + u32 relativeOffset`
	- record payloads starting at those relative offsets
- For sampled `.mob` entries, `dataOffset` points to a second self-describing region with:
	- `u32 outerSize`
	- `u16 itemCount`
	- `u32 itemOffsets[itemCount]`
	- for each item, `u32 payloadSize + payload`
- These two-region findings are verified on at least `MAIN.MOB` entry `0` and `MISSILE.MOB` entry `0`, and the same 40-byte top-level record layout produces clean names and self-consistent region spans across the rest of those files.

## Confirmed Archive Header Layout
- Files start with a fixed `0x408` byte header.
- The 32-bit little-endian field at file offset `0x404` contains:
	- High word: active entry count.
	- Low word: an archive-specific marker, not a global constant.
- Sampled `.spb` low-word markers now observed: `0x0053`, `0x008B`, `0x009F`, `0x00B7`, and `0x00FF`.
- The first 16-bit value at file offset `0x0000` is the archive type code:
	- `0x2711` for sampled `.spb` files.
	- `0x2712` for sampled `.mob` files.
- Immediately after the fixed header, sampled `.spb` files use an entry table with `entryCount` records of `36` bytes each.
- In those `.spb` records, the trailing 32-bit value at record offset `0x20` is the entry data offset.
- Sampled `.mob` files instead use `40` byte top-level records with two trailing dwords at offsets `0x20` and `0x24`.

## Verified Header Palette Candidate
- Sampled `.spb` headers contain a sample-backed embedded palette region that fits best as `255` `RGBX` entries starting at file offset `0x06`.
- The strongest evidence for `0x06` instead of `0x02` or `0x04` is decoded image behavior:
	- `DISKRES.SPB` uses indices `128` and `129` as dominant black/white UI colors only when interpreted from `0x06`.
	- The same alignment yields plausible skin tones and grayscale ramps for `BFACE.SPB` portraits.
	- Shifting the palette start to `0x02` or `0x04` turns those same images into obviously wrong magenta-heavy output.
- Across the current decoded `.spb` sample set, the highest observed palette index is `254`; index `255` is not used.
- `Scordato` now treats this as `255` valid entries at `0x06`, with index `255` left unset.
- The bytes at `0x02..0x05` and `0x402..0x403` vary across archives and are still unresolved. This means the fixed header is not yet fully solved, even though the palette alignment itself is now sample-backed.

## Verified Sample Header Values
- `FACE.SPB`: `dword@0x404 = 0x005300B7`, so `entryCount = 83`.
- `MAGIC.SPB`: `dword@0x404 = 0x002A00B7`, so `entryCount = 42`.
- `MAIN.MOB`: `dword@0x404 = 0x001700B7`, so `entryCount = 23`.

## Verified `.spb` Entry Block Layout
- For sampled `.spb` entries, the block at `entry.dataOffset` is structured as:
	- `u32 outerSize`
	- `u16 subresourceCount`
	- `u32 subresourceOffsets[subresourceCount]`
	- For each subresource: at `blockStart + subresourceOffset`, there is `u32 payloadSize` followed immediately by `payloadSize` bytes of payload.
- This was verified against adjacent entry boundaries in the samples:
	- `MAGIC.SPB` entry `0` at `0x09F0`: `outerSize = 0x1A1`, `subresourceCount = 1`, first subresource offset `0x0A`, first payload size `0x197`, next entry at `0x0B95`.
	- `FACE.SPB` entry `0` at `0x0FB4`: `outerSize = 0x1605`, `subresourceCount = 1`, first subresource offset `0x0A`, first payload size `0x15FB`, next entry at `0x25BD`.
- In both cases, `nextEntryOffset - entryOffset = outerSize + 4`, and `nextEntryOffset - (entryOffset + firstSubresourceOffset + 4) = payloadSize`.

## Verified `.mob` Top-Level Layout
- Sampled `.mob` files share the fixed `0x408` header, the archive type code field, and the `dword@0x404` entry-count/header-marker tail, but they do not share the `.spb` 36-byte top-level record format.
- The verified `.mob` top-level record size is `40` bytes.
- At the top level, the first trailing dword (`metadataOffset`) points to a metadata block whose `outerSize + 4` matches the block span.
- The second trailing dword (`dataOffset`) points to a separate data block. In sampled files, those `dataOffset` values are monotonic and each pointed-to block is also self-describing.

## Verified `.mob` Inner Regions
- Sampled `.mob` metadata regions are name-addressed, not plain offset tables.
- Example: `MISSILE.MOB` entry `0` at `metadataOffset = 0x0B10` begins with `outerSize = 1462`, `recordCount = 10`, then ten records of `name[32] + relativeOffset` such as `피라팔방향`, `피라상`, `피라좌상`, and `피라폭발`.
- Example: `MAIN.MOB` entry `0` at `metadataOffset = 0x07A0` begins with `outerSize = 8242`, `recordCount = 53`, then named records such as `스탠드`, `핀치`, `데미지`, and `죽음`.
- Sampled `.mob` data regions reuse the same outer packing pattern as verified `.spb` entry blocks: `u32 outerSize`, `u16 itemCount`, `u32 itemOffsets[itemCount]`, then `u32 payloadSize + payload` per item.
- Example: `MISSILE.MOB` entry `0` at `dataOffset = 0x585E` has `outerSize = 16579`, `itemCount = 45`, and its first item offset is `0x00BA`, which equals `6 + (45 * 4)`.
- Example: `MAIN.MOB` entry `0` at `dataOffset = 0x2D5FA` has `outerSize = 169075`, `itemCount = 146`, and its first item offset is `0x024E`, which equals `6 + (146 * 4)`.
- The payload semantics of those `.mob` data items are still unresolved. Their payloads do not start with the verified `.spb` image header shape.

## Runtime-Backed MOB Consumer Findings
- `00013e0c` is the active MOB archive initializer. It reads `word@0x406` as the top-level entry count and allocates `entryCount * 40`, which is the binary-side confirmation of the `40` byte top-level record size.
- `00014022` and `00014094` are paired region fetchers over that loaded table:
	- `00014022` seeks to `entry[entryIndex].metadataOffset` at top-level offset `+0x20`.
	- `00014094` seeks to `entry[entryIndex].dataOffset` at top-level offset `+0x24`.
- Both region fetchers first read the on-disk `u32 outerSize`, allocate `outerSize + 4` bytes, store that total size into the first dword of the returned buffer, then copy the raw region bytes immediately after it. This is consistent with the sample-backed `outerSize + 4` span relationship already observed from files.
- `000141eb` is the metadata-record loader for a chosen top-level entry. It seeks into the `metadataOffset` region, optionally searches the `0x24`-byte named record descriptors, then reads the pointed-to record payload as:
	- `u16 elementCount`
	- `u16 unknown`
	- `elementCount * 20` bytes of element records
- `00014714` post-processes those `20` byte metadata elements by grouping on the word at element offset `+6`.
- `000144f0` is the bridge from metadata into the `dataOffset` region. It treats the grouped word from metadata offset `+6` as a data-item index, looks up that item in the `dataOffset` block's `u32 itemOffsets[]` table, then reads a fixed `0xA8` byte header plus a variable tail for that item.
- Runtime/file reconciliation for sampled extracted `item_*.bin` payloads:
	- the runtime loader starts at the item's leading `u32 payloadSize`, while Scordato's `item_*.bin` starts immediately after that size dword.
	- because of that 4-byte shift, the runtime field at offset `0xA4` corresponds to file payload offset `0xA0` in `item_*.bin`.
	- sampled `MISSILE` and `MAIN` items satisfy `filePayloadSize + 4 = 0xA8 + u32@file+0xA0`.
- For sampled `MISSILE.MOB` data items, there is now a sample-backed sprite decode path:
	- decoded width = `u16@file+0x04 + 1`
	- decoded height = `u16@file+0x06 + 1`
	- `u32@file+0xA4` matches the decoded width
	- `u32@file+0xA8` matches the decoded height
	- the row-offset table starts at file payload offset `0xB0`
	- each row stream starts at `&rowOffset[row] + rowOffset[row]`
	- row command opcodes match the verified SPB mode-0 encoding: `0x80..0xFF` literal run, `0x40..0x7F` transparent skip, `0x00` row end
- Example decoded `MISSILE.MOB` entry `0` item payloads:
	- `item_000.bin` decodes to `5 x 10`
	- `item_001.bin` decodes to `6 x 12`
	- `item_004.bin` decodes to `3 x 24`
- `MISSILE.MOB` entry `0` record `피라팔방향` is still a real structural outlier in the file: its item indices `4, 9, 14, 19, 24, 29, 34, 39` match the terminal frame of the eight following directional records `피라상` through `피라우상`.
- The same structural pattern also shows up outside the original MISSILE sample. Example: `EFFECT.MOB` entry `34` (`소울라이트닝`) record `활팔방향` has eight elements `0, 3, 6, 9, 12, 15, 18, 21`, and those indices reappear at a consistent slot inside the next eight directional records.
- That file-level pattern is no longer treated as a runtime classifier. The newer trace shows the engine choosing record indices explicitly in caller state machines, so a file-only selector/sequence split is not source-backed even when the structural pattern is real.
- The second `u16` in metadata record payloads is still unresolved. The loader clearly uses the first `u16` as the count of `20` byte metadata elements, but no equally strong interpretation is yet verified for the second word.

## Image Payload Status
- The start of sampled `.spb` payloads has a repeated, verified structure:
	- `u32 width`
	- `u32 height`
	- `u32 dataStart`
	- `u32 rowMetadata[height]`
	- compressed / encoded image data starting at `dataStart`
- This header shape is verified in at least these samples:
	- `MAGIC.SPB` entries `0` to `3`: `width = 24`, `height = 24`, `dataStart = 108`, and `108 = 12 + (24 * 4)`.
	- `FACE.SPB` entry `0`: `width = 67`, `height = 57`, `dataStart = 240`, and `240 = 12 + (57 * 4)`.
- `00010243` confirms that `rowMetadata[row]` is a self-relative offset from the address of that row's metadata dword to the start of that row's command stream. In other words, the decoder computes `rowStream = &rowMetadata[row] + rowMetadata[row]`.
- The decoded command stream is now source-backed:
	- `0x80..0xFF`: copy `opcode & 0x3F` literal 8-bit pixel indices from the payload stream.
	- `0x40..0x7F`: skip `opcode & 0x3F` transparent pixels in the output.
	- `0x00`: end the current row.
- In the unclipped draw path, `00010243` can also stream directly from `payload + dataStart` and use explicit `0x00` row terminators. In the clipped path, it uses the self-relative per-row table to jump straight to each row's command stream.
- The decoder writes 8-bit indexed pixels directly into the 320x200 framebuffer, so palette application is external to the `.spb` payload format itself.
- A quick decoder check against dumped sample payloads succeeds for `MAGIC` entry `0`, `BFACE` entry `0`, and `DISKRES` entry `0` with no row overrun or opcode mismatch.
- `BASEPAL.PAL` shares a large header prefix with some sampled `.spb` files, but its full relationship to the archive and image decoding path is not yet confirmed.
- `BASEPAL.PAL` is not a raw `256 * 3` VGA file at offset `0`; its total size is `0x432` bytes. The first `0x300` bytes at file offset `0x06` match the embedded header palette bytes in `OBJECT.MOB`, `MAIN.MOB`, and `EFFECT.MOB` exactly.
- That means `BASEPAL.PAL` is source-backed evidence for a shared base palette blob, but it does not by itself solve the `OBJECT.MOB` magenta problem: using `BASEPAL.PAL` instead of the `OBJECT.MOB` header palette produces the same palette indices and colors for those files.

## Other Confirmed Formats
- `TIT.PAL` is exactly `768` bytes, which matches a raw `256 * 3` VGA palette.
- `TIT.RAW` is exactly `307200` bytes, which matches a `640 * 480` 8-bit image.

## Current Probe Tooling
- `Scordato` now parses sampled `.spb` / `.mob` archives, emits manifests, decodes CP949 entry names when possible, and writes PNG outputs under `TestOutput/Scordato`.
- `Scordato` uses the embedded header palette candidate automatically for decoded `.spb` / `.mob` PNG output unless an explicit palette override is supplied.
- `Scordato --palette` now accepts either a raw `768`-byte VGA palette (such as `TIT.PAL`) or a `0x2711` / `0x2712` file carrying an embedded header RGBX palette (such as `BASEPAL.PAL` or sampled archives). Validation run: `OBJECT.MOB` completed successfully with `--palette BASEPAL.PAL`, and the manifest recorded `"embedded header RGBX palette from BASEPAL.PAL"` as the decode source.
- If a `--palette <path>` argument is supplied, that explicit `768` byte raw VGA palette overrides the embedded header palette for PNG output.
- Current `Scordato` default mode is intentionally low-clutter: it writes manifests, decoded PNGs, MOB item PNGs, and explicit-element aligned frame canvases under `metadata/frames/<record-name>/`, but it does not write raw `.bin` dumps or strip PNGs unless requested.
- `Scordato` now treats `runtime-explicit-element-y-minus-height` as the default primary MOB placement mode. The older `runtime-sequence-relative` export is now opt-in and, when enabled, is emitted as the alternate alignment under `metadata/frames-sequence/<record-name>/`.
- `Scordato` command-line switches for the noisier legacy-style output are now:
	- `--emit-binaries` to restore raw archive / region / payload / indexed / mask `.bin` outputs.
	- `--emit-strips` to restore strip PNG export for the enabled alignment modes.
	- `--emit-sequence-relative` to additionally emit the sequence-relative alignment as a secondary view.
- `Scordato` now treats metadata placement words `1` and `2` as signed 16-bit values. This fixes `OBJECT.MOB`, whose `entry_428/meta_000_ANI_0` record previously blew up into a multi-gigabyte strip canvas when `0xFFFF` was misread as `65535` instead of `-1`.
- `Scordato` now skips oversized strip PNGs instead of terminating the whole run. On the current `OBJECT.MOB` sample, `entry_428/meta_000_ANI_0` still gets per-frame exports, but the combined strip is intentionally omitted because its `490247 x 466` canvas is not practical.
